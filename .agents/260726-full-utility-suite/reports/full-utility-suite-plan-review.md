**VERDICT:** PASS_WITH_RISK - plan is mostly aligned with current code and the selected practical subset, but it has three implementation-grade gaps that can produce false-green tests, telemetry privacy drift, or wrong top output.

[HIGH] .agents/260726-full-utility-suite/phase-04-awk-mini-language.md:70 - The plan claims the current shell has no parameter expansion in double quotes, but executor expands every token before dispatch (`cells/tools/shell/src/executor.rs:656`, `cells/tools/shell/src/executor.rs:439`), so the promised `awk -F, "$2 >= 10 { print NR, $1 }"` can lose `$1/$2` before mini-AWK sees it. Correct phase 1/4 to preserve quote metadata or add an AWK-specific no-expansion path plus regression tests for `$1`, `$2`, `$0`, `$?`, and function positional args.
[HIGH] .agents/260726-full-utility-suite/phase-05-getprocs2-and-top.md:36 - Telemetry is meant to be explicit-only, but the current runtime helper grants `GetProcs` automatically to any `spawn=true` app (`libs/ostd/src/runtime.rs:45`, `libs/ostd/src/runtime.rs:82`, `libs/ostd/src/runtime.rs:91`). Correct the plan to add `GetProcs2` only to explicit `declare_syscalls!` users such as shell (`cells/tools/shell/src/main.rs:24`) and add tests proving `app_syscall_set(..., spawn=true)` does not include bit 55.
[MED] .agents/260726-full-utility-suite/phase-05-getprocs2-and-top.md:31 - `ProcessInfoV2` is specified with `usize`-shaped fields by implication from v1, but Cellos targets RV64/AArch64/x86_64 and standards require multi-arch ABI awareness (`docs/code-standards.md:53`). Correct the plan to define exact `#[repr(C)]` fixed-width field types, alignment/padding, and `size_of/align_of` ABI tests for both v1 and v2.
[MED] .agents/260726-full-utility-suite/phase-05-getprocs2-and-top.md:25 - CPU% is underspecified for the existing multi-hart scheduler (`kernel/src/task/scheduler.rs:76`, `kernel/src/task/scheduler.rs:792`); dividing per-task delta ticks by wall-clock sample ticks will be wrong once more than one hart runs. Correct the plan to define either per-core percent or total-capacity percent using elapsed ticks times online hart count, and expose/test the denominator used by `top`.
[MED] .agents/260726-full-utility-suite/phase-01-argv-and-pattern-engine.md:33 - ERE-lite promises classes, groups, alternation, and bounded quantifiers while only saying "linear-time" (`phase-01-argv-and-pattern-engine.md:26`); without explicit NFA/DFA state/depth/repetition caps, regex-like patterns can still become allocation or CPU DoS. Correct the plan to require a Thompson-style VM or compiled automaton with max states, max AST depth, max repetition expansion, and tests for nested alternation/quantifier rejection.
[LOW] .agents/260726-full-utility-suite/phase-01-argv-and-pattern-engine.md:54 - Status 0/1/2 cannot survive the current `ViResult<()>` dispatch because errors collapse to `1` (`cells/tools/shell/src/executor.rs:983`). The plan mentions status plumbing, but should make the correction concrete: introduce a utility status return type and migrate grep/sed/awk/top dispatch before phase 2 relies on status 2.
[POSITIVE] .agents/260726-full-utility-suite/plan.md:30 - Law 1 gate is accurate: `docs/code-standards.md:14` requires two explicit confirmations for `libs/api`, and the plan records one confirmation received with the second pending.
[POSITIVE] .agents/260726-full-utility-suite/phase-05-getprocs2-and-top.md:30 - The plan correctly preserves `GetProcs=30` and old `ProcessInfo`; current ABI maps opcode 30 to `GetProcs` (`libs/api/src/abi/syscall.rs:351`, `libs/api/src/abi/syscall.rs:634`) and v1 rows are exactly id/state/name (`libs/api/src/abi/syscall.rs:759`).
