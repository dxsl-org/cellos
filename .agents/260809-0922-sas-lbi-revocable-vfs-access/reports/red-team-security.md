**VERDICT:** BLOCKED - the package chooses the right direction, but two plan corrections are needed before implementation can be treated as fail-closed.

[HIGH] .agents/260809-0922-sas-lbi-revocable-vfs-access/phase-05-migrate-callers-and-retire-dataptr.md:26 - Runtime fallback to `GetFile -> DataPtr` keeps the unrevocable SAS escape alive exactly when the new bounded/handle path fails; source confirms `GetFile` returns permanent raw authority (`cells/services/vfs/src/dispatch.rs:55`, `cells/services/vfs/src/dispatch.rs:56`, `libs/api/src/services/ipc.rs:200`). Remove automatic fallback from the plan: rollback may re-enable the old path as an explicit operator/code rollback, but migrated reads must return typed errors on denial, truncation, service death, or malformed replies and must never silently fall back to `DataPtr`. Undo by reverting the Phase 05/06 plan wording; cannot undo any external release that documents fallback as supported.
[HIGH] .agents/260809-0922-sas-lbi-revocable-vfs-access/phase-03-lifecycle-cleanup-checkpoint.md:26 - The plan treats `Scheduler::exit_task` as the common death proof, but cleanup parity is outside `exit_task`: Exit/ForceExit revoke `CAP_TABLE` (`kernel/src/task/syscall.rs:2055`, `kernel/src/task/syscall.rs:2151`) while fault and heartbeat/watchdog paths call `exit_task` plus quota/resource/grant cleanup without `CAP_TABLE.revoke_all_for` (`kernel/src/task.rs:597`, `kernel/src/task.rs:602`, `kernel/src/task/scheduler.rs:843`, `kernel/src/task/scheduler.rs:981`). Correct Phase 03 to require one audited terminal cleanup helper or an explicit matrix proving each death path revokes kernel caps, VFS handles, pending reads, grants, fast-IPC registration, and quarantines before Phase 04; also state that kernel `OpenCap` cannot be used as a revocable VFS substitute until this gap is closed. Undo by reverting the lifecycle plan change; cannot undo stale capabilities already exposed in a runtime that ships the incomplete funnel.
[MED] .agents/260809-0922-sas-lbi-revocable-vfs-access/phase-04-file-handle-bounded-reads.md:30 - Durable file-handle lifetime allows "generation successor purge" as a terminal case, but the existing VFS `HandleTable` has only per-handle owner removal and no cell/generation purge (`cells/services/vfs/src/handle_table.rs:42`, `cells/services/vfs/src/handle_table.rs:99`); only directory state currently has `purge_cell` (`cells/services/vfs/src/dirs/lifecycle.rs:175`). Make Phase 04 require a `purge_owner`/`purge_cell_generation` path for file handles and pending reads, with bounded table growth tests, or explicitly keep file handles disabled when Phase 03 selects lazy containment only.
[MED] .agents/260809-0922-sas-lbi-revocable-vfs-access/phase-02-copy-out-compatibility-adapter.md:52 - The copy-out helper step does not specify how it preserves authorization parity between `Stat`, `ReadFileGrant`, and `ReadAsync+Poll`; current dispatch denies sealed path-addressed requests before arms (`cells/services/vfs/src/dispatch.rs:50`) and `ReadFileGrant` authorizes before resolving the grant (`cells/services/vfs/src/dispatch.rs:292`, `cells/services/vfs/src/dispatch.rs:295`). Add an implementation requirement that size discovery and read use the same attested VFS path and collapse denied/missing/truncated states to typed errors, not partial success or a second read path with weaker seal handling.
[LOW] .agents/260809-0922-sas-lbi-revocable-vfs-access/phase-01-freeze-decision-contract.md:46 - The grep gate only names `VfsResponse::DataPtr`, so it can miss new raw-pointer producers such as direct `get_file_ptr` calls or fast `GetFile` serving while still satisfying the text gate; source shows the fast handler can still emit `DataPtr` (`cells/services/vfs/src/main.rs:125`, `cells/services/vfs/src/main.rs:135`). Expand the gate to include `VfsRequest::GetFile`, `DataPtr`, `get_file_ptr`, and fast-VFS `GetFile` arms outside VFS/tests/bench until Law 1 removal.
[POSITIVE] .agents/260809-0922-sas-lbi-revocable-vfs-access/plan.md:61 - The scope stop correctly rejects identity-less fast IPC; current fast IPC derives caller identity from scheduler state (`kernel/src/fast_ipc.rs:126`, `kernel/src/fast_ipc.rs:152`) and the VFS fast handler refuses unknown/unmet/sealed callers before `DataPtr` (`cells/services/vfs/src/main.rs:122`, `cells/services/vfs/src/main.rs:127`, `cells/services/vfs/src/main.rs:133`).
[POSITIVE] .agents/260809-0922-sas-lbi-revocable-vfs-access/phase-04-file-handle-bounded-reads.md:57 - Owner/scope design matches existing handle-table defenses: handles are keyed by shared ids but every lookup compares `Caller { cell, generation }`, so guessing or wrong-owner probes are indistinguishable from unknown handles (`cells/services/vfs/src/caller.rs:17`, `cells/services/vfs/src/handle_table.rs:80`, `cells/services/vfs/src/pending.rs:85`).
