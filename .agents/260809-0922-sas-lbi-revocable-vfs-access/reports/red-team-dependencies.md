**VERDICT:** BLOCKED — the plan direction is credible, but implementation order still depends on missing caller authority, directory-handle acquisition, and shared lifecycle work that must be resolved before ABI publication.

[HIGH]     .agents/260809-0922-sas-lbi-revocable-vfs-access/phase-04-file-handle-bounded-reads.md:24 — Phase 04 makes `OpenFileAt { dir, name }` depend on an existing directory handle, while Phase 05 migrates path-string callers to handle reads without adding a prerequisite for those callers to acquire/seal directory handles; current `OpenRootDir` is itself path-addressed and must precede `SealPaths` (`libs/api/src/services/ipc.rs:98`, `libs/api/src/services/ipc.rs:102`), and repo grep shows `OpenRootDir`/`ReadAt` production callers are absent outside VFS/tests (`cells/services/vfs/src/dispatch_dirs.rs:40`, `cells/tests/vfs-test/src/dircap.rs:66`). Exact correction: add a Phase 04 prerequisite/step that defines each production caller's dir bootstrap (`OpenRootDir` before seal, inherited dirs, or separately approved absolute opener) before `GetFile/DataPtr` retirement. Rollback: keep Phase 02 copy-out and do not publish file-handle ABI; irreversible part: externally consumed append-only variants cannot be silently redesigned after release.
[HIGH]     .agents/260809-0922-sas-lbi-revocable-vfs-access/phase-05-migrate-callers-and-retire-dataptr.md:53 — Caller discovery still excludes bounded VFS readers that are already production dependencies: HTTPD serves static files through `net_ipc::vfs_read_file` (`cells/services/httpd/src/handlers.rs:134`), and both service HTTPD and net-tools HTTPD use `ReadAsync`/`Poll` (`cells/services/httpd/src/net_ipc.rs:126`, `cells/tools/net-tools/src/bin/httpd.rs:68`). Exact correction: make Phase 01/05 inventory a full VFS-read-surface gate (`GetFile`, `DataPtr`, `ReadAsync`, `Poll`, `ReadAt`, `ReadFileGrant`, helper names, generated cells), and add HTTPD smoke tests before any no-production-reader claim. Rollback: restore affected callers to their prior `ReadAsync+Poll`; irreversible part: none before ABI removal.
[HIGH]     .agents/260809-0922-sas-lbi-revocable-vfs-access/phase-02-copy-out-compatibility-adapter.md:53 — Preferred `ReadFileGrant` migration depends on GrantCap, but several named migration targets currently declare no grant syscalls: Lua only `Send, Recv, Log, Heartbeat` (`cells/runtimes/lua/src/main.rs:11`), WASM only `Send, Recv, Log, Heartbeat` (`cells/tools/wasm/src/main.rs:14`), Hypha fs only `Send, Recv, Log, LookupService` (`cells/apps/hypha/tools/fs/src/main.rs:28`), net-broker lacks GrantCap in its explicit allowlist (`cells/services/net-broker/src/main.rs:43`), and HTTPD lacks GrantCap (`cells/services/httpd/src/main.rs:25`); Grant APIs require bit 39 (`libs/api/src/abi/syscall.rs:661`, `libs/ostd/src/runtime.rs:43`). Exact correction: split callers into grant-capable versus inline-reply/dir-read paths, or add explicit syscall/manifest authorization changes plus tests per caller before selecting `ReadFileGrant`. Rollback: revert allowlist widening and use typed inline reads; irreversible part: published widened syscall authority if released.
[MED]      .agents/260809-0922-sas-lbi-revocable-vfs-access/phase-03-lifecycle-cleanup-checkpoint.md:51 — The plan names pin/table teardown order but does not bind Phase 04/06 to the existing Async Pinning Registry and cap-revocation precedence rulings; prior plans say async pin/quarantine owns mechanism/lifecycle ordering and selective cap revoke must call the same pin-aware reclaim path (`.agents/260727-2101-midori-lessons-cellos/phase-07-async-reactor.md:13`, `.agents/260712-1901-cap-revocation/phase-02-selective-grant-reclaim.md:11`), and only `GrantDma` is currently wired as a pin producer (`.agents/260727-2101-midori-lessons-cellos/phase-07-async-reactor.md:463`). Exact correction: add those two plans as explicit dependencies/consumed interfaces for any VFS grant/cancel/revoke work, and forbid a second VFS-local pin/reclaim policy.
[MED]      .agents/260809-0922-sas-lbi-revocable-vfs-access/phase-04-file-handle-bounded-reads.md:61 — Service-side revoke for "all handles owned by a `Caller` or `CellId` generation" assumes purge APIs that exist for directory handles but not file handles or pending reads: `DirTable::purge_cell` is called on higher-generation contact (`cells/services/vfs/src/dirs/lifecycle.rs:61`), while `HandleTable` exposes only per-cap owner lookup/removal (`cells/services/vfs/src/handle_table.rs:83`, `cells/services/vfs/src/handle_table.rs:104`) and `PendingTable` exposes only per-handle poll (`cells/services/vfs/src/pending.rs:95`). Exact correction: add file-handle and pending-read `purge_owner`/bounded-sweep APIs as Phase 04 owned work or keep durable file handles disabled when Phase 03 selects lazy containment.
[MED]      .agents/260809-0922-sas-lbi-revocable-vfs-access/phase-02-copy-out-compatibility-adapter.md:37 — The whole-file compatibility assumption hides a current 480-byte transport dependency: `Poll` deliberately truncates to 480 bytes because full-frame payloads broke postcard replies (`cells/services/vfs/src/dispatch.rs:196`), and HTTPD/net-tools return empty or partial success on encode/decode/size failures (`cells/services/httpd/src/net_ipc.rs:126`, `cells/tools/net-tools/src/bin/httpd.rs:77`). Exact correction: require `StatAt`/chunked reads or grant path per caller based on max file size, and add truncation-as-error tests before replacing raw `DataPtr`.
[LOW]      .agents/260809-0922-sas-lbi-revocable-vfs-access/phase-04-file-handle-bounded-reads.md:38 — `HandleTable` split is already mandatory, not optional: the file is 194 lines now, so adding a real file-handle endpoint exceeds the local 200-line guidance (`docs/code-standards.md:79`, `cells/services/vfs/src/handle_table.rs:194`). Exact correction: pre-own `cells/services/vfs/src/file_handles.rs` or smaller table submodules in Phase 04 file ownership.
[POSITIVE] .agents/260809-0922-sas-lbi-revocable-vfs-access/plan.md:49 — The Law 1 approval gates are correctly placed before `libs/api/` VFS enum edits and ABI removal, matching the local rule that `libs/api/` and `libs/types/` changes need two explicit confirmations (`docs/code-standards.md:12`).
