**VERDICT:** BLOCKED — late-DMA quarantine can become releasable, cache completion loses the exact byte range, and the framebuffer mapping/write path has rollback and Device-memory correctness hazards.

[HIGH] kernel/src/memory/pin.rs:421 — `CacheSyncEntry` stores only the page-rounded base/count and completion reconstructs that full-page span at lines 443-449, so a subrange operation invalidates unrelated dirty bytes in every touched page instead of the immutable byte range promised by the ABI. Store both exact `base,len` and page-rounded pin coverage, invalidate only the exact line-covered range, and test two independent subranges in one page. Rollback removes the new cache-sync ABI and BCM caller together; already-submitted requests/pins are recoverable only by reboot.
[HIGH] kernel/src/memory/pin.rs:633 — timeout cache-sync frames enter quarantine with the generic `ReleaseKey::Owner`, while `acknowledge` releases every owner-keyed hold at lines 671-678 without checking the still-live cache-sync entry. The current task funnel happens to acknowledge before reap, but any reordered/duplicate acknowledgement can recycle a page that VideoCore may still write. Use a non-releasable cache-sync/boot-lifetime quarantine key (or exact device reset proof), and add reap-before-ack plus duplicate-reap regression tests. Rollback disables BCM and reboots; a page already exposed to VideoCore cannot be reclaimed safely in the current boot.
[HIGH] kernel/src/memory/paging.rs:920 — partial mapping rollback unmaps every earlier page rather than restoring its previous PTE, although AArch64 `PageTable::map` overwrites existing leaves; additionally, syscall.rs:5022 performs check→map→register non-atomically and leaves the mapping installed if registration loses the race. A failure can therefore destroy prior translations or expose an unregistered range despite returning an error. Preflight/allocate and publish in one transaction, snapshot/restore exact leaves, and roll back mapping if registration fails. Rollback disables display registration and reboots; translations already removed cannot be reconstructed reliably without saved PTEs.
[HIGH] kernel/src/memory/paging.rs:898 — framebuffer pages are mapped Device-nGnRnE, but `BcmFramebuffer::flush_rect` writes them through `copy_nonoverlapping` at cells/drivers/bcm-display/src/lib.rs:198, allowing compiler-widened/unaligned normal-memory stores that are not a safe contract for Device memory and can fault or misbehave on Cortex-A53. Add a Normal non-cacheable framebuffer attribute and the required visibility barrier, or use naturally aligned volatile pixel stores with an explicit barrier. Rollback restores the non-launching/safe HDMI image and reboots; the current mapping persists until reboot.
[HIGH] cells/drivers/bcm-display/src/mailbox.rs:63 — a live `&mut [u32]` spans cache-begin, mailbox submission, VideoCore mutation, and cache-complete, violating the exclusive-reference contract and permitting the compiler to reuse pre-DMA values. Perform request/response copies through raw volatile pointers, end all CPU references before Begin, and form/read CPU-side values only after successful Complete. Rollback restores the pre-A+ mailbox transport only in a non-deployed image; any timed-out submitted page remains quarantined until reboot.
[MED] kernel/src/resource_registry.rs:392 — framebuffer ownership is stored from the caller TID (`syscall.rs:5039`) but teardown compares it with the retiring logical `CellId`, and even a matching exit clears only metadata without unmapping the shared USER range. Cleanup may therefore miss the record, or a restarted driver may register a different allocation and accumulate permanently exposed ranges during one boot. Store one identifier domain consistently and keep a per-boot range tombstone with identical-only rebind, or implement exact safe unmap/restore before clearing registration.
[MED] kernel/src/task/syscall.rs:5002 — geometry validation requires `pitch * height <= size` but places no geometry-derived upper bound on `size`; a malformed firmware response can request mapping nearly the entire allocator-to-peripheral aperture and consume page tables/CPU in one syscall. Bound size to the rounded scanout coverage (or a documented maximum) before mapping.
[MED] kernel/src/memory/pin_tests.rs:64 — regression coverage proves only foreign-token rejection and exact-token removal; it does not cover post-timeout task death, reap-before/after-ack ordering, duplicate completion, exact byte-range preservation, partial mapping rollback, or repeated framebuffer registration after owner exit. Add the phase test matrix before producing a physical image.
[LOW] working tree — `git diff` currently spans 40 files and no file is whitespace-only under `git diff -w`; unrelated KMS/VFS/SD edits are interleaved with HDMI work rather than being separable as harmless formatting. Stage the reviewed HDMI paths explicitly and verify the staged diff before commit.
[POSITIVE] cells/drivers/bcm-display/src/main.rs:21 — the BCM cell now declares a narrow syscall allowlist and accepts scanout IPC only from kernel sender TID 0; the structured property response checks also reject missing response bits and reordered tags.
[POSITIVE] verification — driver host tests pass 6/6, AArch64 driver check passes, RPi3 kernel check passes with only the two pre-existing timer dead-code warnings, and `git diff --check` reports no whitespace errors.
