**VERDICT:** PASS_WITH_RISK — all five prior High findings are resolved; remaining risks are bounded API/performance and failure-path concerns rather than release blockers for the BCM physical gate.

[MED] hal/arch/arm/src/aarch64/cache.rs:93 — exact byte metadata is now preserved, but `dc ivac` necessarily invalidates whole boundary cache lines; the public subrange API still accepts unaligned ranges and does not state that adjacent bytes in those lines must remain untouched while DMA owns the operation. Require cache-line-aligned begin ranges, or document/enforce exclusive boundary-line ownership and have BCM synchronize the request length rounded to the CTR-derived line size.
[MED] kernel/src/memory/paging.rs:939 — mapping failure unmaps every newly installed leaf, but direct `PageTable::map` may allocate intermediate tables and this rollback neither prunes nor returns those frames. Repeated failed reservations can leak page-table memory; use `prune_empty` for each rolled-back branch or preallocate the complete page-table transaction.
[MED] kernel/src/task/syscall.rs:5002 — `pitch * height <= size` is enforced but `size` remains unbounded by rounded geometry, allowing malformed firmware metadata to map a much larger reserved aperture and spend disproportionate time/page-table memory. Cap it to rounded scanout coverage or a documented framebuffer maximum.
[MED] cells/drivers/bcm-display/src/lib.rs:190 — correct 32-bit volatile Device writes avoid widened/unaligned accesses, but a full 1280x720 flush now performs 921,600 Device-nGnRnE stores; sustained compositor flushes may monopolize the CPU. Treat the physical 10-minute cursor/flush gate as a performance requirement and consider a reviewed Normal-noncacheable mapping if it fails.
[POSITIVE] kernel/src/memory/pin.rs:128 — cache-sync entries now retain exact operation bytes separately from page pin coverage, and completion returns the exact tuple.
[POSITIVE] kernel/src/memory/pin.rs:641 — cache-sync quarantine uses `CacheSyncBootLifetime`, which no owner/IOMMU acknowledgement matches; tests cover acknowledgement both before and after reaper transfer.
[POSITIVE] kernel/src/resource_registry.rs:463 — Pending/Active reservation serializes publication; Active metadata survives teardown and only identical replay succeeds.
[POSITIVE] kernel/src/memory/paging.rs:918 — preflight rejects existing leaves, so rollback only removes leaves created by this transaction; syscall cancellation clears Pending state on mapping failure.
[POSITIVE] cells/drivers/bcm-display/src/mailbox.rs:61 — the DMA page is accessed through raw pointers only before Begin and after successful Complete; no Rust slice/reference remains live across VideoCore ownership.
[POSITIVE] verification — BCM host tests pass 7/7, AArch64 BCM check passes, RPi3 kernel check passes with only two pre-existing timer dead-code warnings, and scoped `git diff --check` is clean.
