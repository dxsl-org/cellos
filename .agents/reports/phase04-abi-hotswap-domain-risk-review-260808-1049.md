**VERDICT:** PASS_WITH_RISK — no source/test defect found in syscall 400 retirement or hotswap authority, but x86/AArch64 QEMU boot lanes remain host-gated rather than runtime-proven.

[LOW]      .agents/260712-0800-supervisory-cell-migration/phase-04-kernel-cleanup.md:146 — x86 and AArch64 QEMU boot gates are explicitly deferred due host tooling. Accepted gap, not a source defect; keep release-kernel pass separate from boot-lane proof until a working host reruns them.
[POSITIVE] libs/api/src/abi/syscall.rs:840 — public numeric decode maps live 401/410-415/419/421/422 while omitting 400, and the fallback at line 853 returns `Unknown`; raw numeric 400 has no live ABI target.
[POSITIVE] kernel/src/task/syscall.rs:4918 — raw fallback mappings enumerate only legacy FD and block opcodes, then return `None` at line 4953; 400 cannot reach behavior through raw mapping.
[POSITIVE] kernel/src/task/syscall.rs:5000 — true unknown opcodes are denied for allowlist-declaring tasks, and permit-all legacy tasks still hit `map_syscall`'s no-400 `None` path before any handler runs.
[POSITIVE] libs/api/src/abi/syscall_tests.rs:132 — ABI tests lock previously unmapped ids to `Unknown`, including 400 at line 135, and preserve bit 32 for `HotSwapReady`/`Snapshot` at lines 140-142.
[POSITIVE] libs/api/src/abi/syscall.rs:646 — bit 32 is retained only as the compatibility bucket for `HotSwapReady` and `Snapshot`; Snapshot remains allowlist-only here and is re-gated by SupervisorCap at `kernel/src/task/syscall.rs:4047`.
[POSITIVE] kernel/src/cell/hotswap.rs:66 — replacement ceiling consumption requires the source task to still be `Frozen`, making the authority token one-shot and unavailable to stale/non-frozen callers.
[POSITIVE] kernel/src/cell/hotswap.rs:184 — cutover requires frozen source, ready non-frozen target, and matching `hotswap_source_tid`; a random bit-32 `HotSwapReady` caller cannot authorize a different operation.
[POSITIVE] kernel/src/task/syscall.rs:3717 — `SpawnReplacement` still checks SupervisorCap, path bounds, launch profile, frozen ceiling, and replacement binding; failed binding kills the orphaned replacement before returning.
[POSITIVE] cells/services/supervisor/src/hotswap.rs:131 — supervisor rollback resumes the old provider and re-registers the service after replacement spawn failure, and `rollback_spawned` covers restore/ready/commit failures at line 172.
[POSITIVE] .agents/260712-0800-supervisory-cell-migration/phase-04-kernel-cleanup.md:144 — reported reachable final lanes are API 75/75, RV64/AArch64/x86 release-kernel builds, hotswap-smoke 15/15, and launch-profile 1/1; local review reran `cargo test -p api --target x86_64-unknown-linux-gnu syscall` as 17/17 with 58 filtered and `cargo check -p vicell-kernel` green.
[POSITIVE] .agents/260712-0800-supervisory-cell-migration/phase-04-kernel-cleanup.md:147 — regenerated `kernel/src/embedded/init` is intentionally retained as the fresh tracked bootstrap ELF; observed file is RISC-V ELF, size 41552, sha256 `59a6787fab0607686e9dcc56f87bda093d4dcc63abc3b2c82f7582b5b11aa806`.
