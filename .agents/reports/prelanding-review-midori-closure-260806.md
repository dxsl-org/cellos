**VERDICT:** BLOCKED — one trust-boundary issue lets `SpawnFromElf` authorize capability ceilings from a caller-supplied path hint that is not bound to the caller-supplied ELF bytes.

[CRITICAL] kernel/src/task/syscall.rs:2673 — `SpawnFromElf` authorizes `(caller, Elf, path_str)` using a user-supplied `path_hint`, then passes arbitrary caller-owned grant bytes to `spawn_gated`; `kernel/src/loader.rs:261` later derives requested caps/policy from that same hint, while `kernel/src/loader/launch_profile/profiles.rs:26` and `cells/tools/shell/src/main.rs:31` newly allow shell `SpawnFromElf` for reviewed targets. Attack: a caller on an allowed Elf edge can pair bytes not proven to be `/bin/<target>` with a privileged reviewed target hint such as `/bin/httpd`, especially in dev unsigned/legacy-manifest cases. Impact: path-derived caps can be granted to bytes whose identity was not proven to match the path. Fix: bind bytes to the target path before using path-derived caps (kernel-read/measurement match or signed manifest identity), or make `SpawnFromElf` manifest-only and reject legacy/path-derived caps; pre-merge rollback is to remove shell `SpawnFromElf` from the allowlist and keep exec/mem launch fail-closed. Undo scope: code-only rollback; no external data has been written by the diff itself.
[LOW] libs/api/src/abi/syscall.rs:412 — `SpawnSetDirs` still documents and dispatches as lifecycle `SpawnCap`-gated while Phase 04 lets shell launch exact path/ELF children without lifecycle `SpawnCap`; no current wrapper/caller was found, so this is not blocking, but the contract is now easy to misuse when directory inheritance gets re-exposed. Fix: decide whether directory staging follows exact launch-profile authority or remains lifecycle-only, then update the ABI comment and kernel gate together.
[POSITIVE] kernel/src/task/user_out.rs:51 — `GrantSlice` size out-pointers are narrowed to aligned slots in the caller's own user stack, avoiding the previous arbitrary user write surface for grant length returns.

Checklist result: 1 critical, 1 informational.
Scout: reused `.agents/260805-1833-midori-closure-execution/reports/scout-report.md`; grep-filled launch profile, grant/out-pointer, and shell allowlist blast radius.
Verification read: `git diff --check origin/main..HEAD` passed.
Simplification: no `haily:` markers observed in affected files.
