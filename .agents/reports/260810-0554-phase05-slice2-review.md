**VERDICT:** PASS_WITH_RISK — bounded reads and caller caps are directionally correct, but cleanup short-circuits can leak VFS directory handles after a file-close failure.

[HIGH]     libs/ostd/src/clients/vfs/read_file/session.rs:53 — `cleanup()` uses `self.close_file().and(self.close_dirs())`, so any `CloseFile` transport/handle error skips every `CloseDir`; VFS keeps per-caller directory handles capped at 32 (`cells/services/vfs/src/dirs.rs:36`), and repeated failures can exhaust the caller's handle budget. Fix by always running both cleanup phases, preserving the first error; rollback by reverting consumers to the previous non-handle `read_file` path, but any handles already leaked in a live VFS instance cannot be reclaimed except by explicit close, caller purge, VFS restart, or cell generation replacement.
[POSITIVE] libs/ostd/src/clients/vfs/read_file/session.rs:103 — bounded reads request at most `MAX_READ_CHUNK` and probe exact-limit files with one extra byte, so payloads over the caller limit return `ViError::OutOfMemory` instead of being silently truncated.
[POSITIVE] cells/services/net-broker/src/transport.rs:82 — K1 loading requests only 64 bytes, rejects short files, and preserves the first 32 bytes for the PSK.
[POSITIVE] cells/apps/hypha/tools/fs/src/main.rs:150 — Hypha keeps the 64 KiB read ceiling separate from the 2048-character tool-result cap and preserves the binary fallback.
