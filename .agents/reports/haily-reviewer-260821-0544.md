**VERDICT:** BLOCKED — the design gate can approve Tier 2 without proving that hostile syscall pointers cannot fault the kernel or that an active domain root is quiesced before its page-table frames are freed.

[HIGH] docs/specs/22-native-domain-cell-implementation-gate.md:94 — The only user-memory boundary specified is copied IPC, while the negative matrix at lines 153–164 has no hostile syscall-pointer case; current `validate_user_buf` checks only NULL/length/overflow (`kernel/src/task/syscall.rs:612-632`), many syscall arms then dereference raw user pointers (for example `kernel/src/task/syscall.rs:2175-2185`), and kernel-mode faults panic (`hal/arch/riscv/src/rv64/trap.rs:124-151`, `kernel/src/memory/paging.rs:938-970`). Require domain-aware `copy_from_user`/`copy_to_user` validation and fault recovery across the entire syscall ABI, plus invalid, unmapped, cross-page, kernel, peer-domain, and concurrent-unmap pointer tests; narrow the categorical claim at `docs/specs/18-cell-trust-tiers.md:135` to direct CPU-memory mappings. Rollback: revert Spec 22 and its three cross-reference edits; no runtime state or data is irreversible.

[HIGH] docs/specs/22-native-domain-cell-implementation-gate.md:48 — The mandatory teardown order allows fault/exit cleanup to revoke, invalidate, and free domain table frames without first proving that every hart—including the faulting hart currently executing under that root—has switched to a safe root; line 89 prevents a second-hart run but does not quiesce an already active root. Require a DYING state, removal from all run queues, safe-root activation on every hart, generation-matched acknowledgements that no hart still names the domain, and deferred fault-path destruction before freeing the root last; add a forced-exit-during-syscall/migration test. Rollback: revert Spec 22 and its three cross-reference edits; no runtime state or data is irreversible.

[MED] docs/specs/22-native-domain-cell-implementation-gate.md:172 — `native-domains` is defined as a compile-time kernel feature, but lines 178–182 promise admission rollback through a “runtime feature flag” without requiring a separate boot/runtime admission control, its atomicity, or persistence semantics. Define distinct build capability and default-off runtime policy controls, then specify whether rollback requires reboot and how pending/concurrent admissions observe the disable.

[LOW] docs/specs/22-native-domain-cell-implementation-gate.md:203 — “All loader paths share signature/manifest admission” is not literally true: bootstrap init calls `task::spawn_from_mem` directly at `kernel/src/main.rs:877`, bypassing `loader::spawn_gated`. Scope the evidence claim to governed native loader/syscall paths and explicitly name the trusted bootstrap exception.

[POSITIVE] docs/security-model.md:111 — The updated security posture accurately separates signature admission from containment and explicitly states that absent signatures still enter the SAS when `signing-required` is disabled; `kernel/src/loader.rs:120-153` confirms that behavior.
