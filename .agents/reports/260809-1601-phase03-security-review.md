**VERDICT:** BLOCKED — two VFS lifecycle edges can leave request authority or owner state alive past the event that should revoke it.

[HIGH]     kernel/src/task/syscall.rs:2466 — `NotifyOnExit` decides the watched task is alive under `SCHEDULER`, drops that lock, then subscribes at line 2468; if the owner exits in that gap, VFS never receives the death and `VfsManager::watched_owners` state created before `cells/services/vfs/src/main.rs:212` can remain until a later same-cell generation contact. Fix by subscribing while still holding `SCHEDULER` or by rechecking liveness atomically inside the subscribe helper. Rollback: revert the Phase03 VFS owner-watch changes; already-issued runtime handles cannot be purged retroactively.
[HIGH]     kernel/src/task/syscall.rs:1509 — `finish_vfs_send_release` releases the exact VFS lease but does not clear the VFS task's `current_caller_*`; because `current_vfs_grant_lookup` treats any registered VFS-cell task with `current_caller` set as active at lines 697-705, a stale request context can authorize later `GrantSlice` calls until the next receive overwrites it. Fix by clearing the VFS caller context after the send/release terminal point, and add a regression where a post-reply `GrantSlice` is denied. Rollback: revert the Phase03 VFS lease pinning path; any stale grant copies already made cannot be undone.
[POSITIVE] kernel/src/memory/pin.rs:462 — VFS lease release matches holder, grant owner, and request generation, so wrong-owner and stale-generation releases do not clear a live lease.
[POSITIVE] cells/services/vfs/src/pending.rs:95 — pending-read polling checks ownership before removal, so handle guessing does not consume another cell's result.
[POSITIVE] cells/services/vfs/src/handle_table.rs:104 — close/remove checks the caller before deleting an entry, preventing cap sweeps from destroying another cell's handle.
[POSITIVE] kernel/src/task/scheduler.rs:566 — dead VFS-holder cleanup is deferred via `pending_vfs_lease_release`, keeping frame-free paths out of the scheduler lock.

Verification: `cargo check -p vicell-kernel` passed. `cargo test -p service-vfs --target x86_64-unknown-linux-gnu` is not a valid host lane here and failed with `unwinding panics are not supported without std`. `cargo test -p vicell-kernel pin --target x86_64-unknown-linux-gnu` also hit the known no-std test harness conflict plus `SpinLockGuard`/`SpinlockGuard` spelling in the test-only import path.
