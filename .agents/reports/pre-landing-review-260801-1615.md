**VERDICT:** BLOCKED — one completion-queue race can wake the wrong task; 1 critical, 2 informational.

[CRITICAL] kernel/src/task/completion_wait.rs:65 — `WaitCompletion` stores `caller_id` as the queue waiter before `reserve()` and never clears it on `TryAgain`, immediate-completion, timeout, or spurious-return paths; a later completion can make an unrelated parked state runnable because `deliver_pending_wakes` wakes any non-ready/non-running state at `kernel/src/task/completion.rs:340`. Fix by registering only after a slot is reserved and the task is about to park, and clear the waiter on every return path; rollback is to disable `ViSyscall::WaitCompletion`/NET_RX completion migration or revert the completion-queue commits, with no irreversible on-disk state.
[MED] .github/workflows/ci.yml:177 — kernel CI builds the default dev feature set and grep found no workflow lane passing `--features signing-required`, so unsigned-denial behavior in `kernel/src/loader.rs:139` is not compiled or boot-tested by CI. Add at least one compile/runtime negative lane for `signing-required` with unsigned, stripped, wrong-key and tampered ELFs.
[MED] kernel/src/loader/wx.rs:28 — W^X lowering explicitly does not invalidate stale cross-hart translations, and `kernel/src/memory/page_protect.rs:14` says `protect_page` has no shootdown; on SMP or VA reuse this can leave a prior writable translation live on another hart. Add an SMP shootdown gate or keep the feature documented and tested as single-hart-only until that lands.
[POSITIVE] scripts/test_cellos_sign.py — 35 admission/signing unit tests pass locally.
[POSITIVE] libs/api/src/abi/syscall.rs:624 — `cargo test -p api --target x86_64-unknown-linux-gnu` passed 69 unit tests plus contract tests, including the new ABI/discriminant checks.
