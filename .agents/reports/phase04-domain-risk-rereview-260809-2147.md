**VERDICT:** RISKS_NOTED — scoped Phase04 file-handle ABI/service/docs/tests are production-ready against the requested attack set; residual risk is verification-lane substitution because `cargo test -p service-vfs` is not a usable host unit lane here.

[POSITIVE] libs/api/src/services/ipc.rs:153 — Phase04 wire delta is append-only: `OpenFileAt`, `ReadFileHandle`, and `CloseFile` sit after `SealPaths`; API tests confirm old discriminants plus new request variants 23-25 and response variant 9.
[POSITIVE] cells/services/vfs/src/file_handles/table.rs:84 — guessed/stale/wrong-owner file handles are rejected before path disclosure, and wrong-owner `read`/`close` leaves the real owner entry intact.
[POSITIVE] cells/services/vfs/src/file_handles/table.rs:66 — file-handle IDs start nonzero, never reuse after close, and fail closed on `u64` exhaustion instead of wrapping.
[POSITIVE] cells/services/vfs/src/dispatch_dirs.rs:151 — `OpenFileAt` requires an owning caller, resolves with the existing directory-name validator, checks current read policy, rejects directories, and stores only service-local owner/path/parent state.
[POSITIVE] cells/services/vfs/src/dispatch_dirs.rs:185 — `ReadFileHandle` owner-checks, re-authorizes current read policy, clamps `max` to `IPC_BUF_SIZE - 96`, handles zero/EOF reads as empty `Data`, and copies only from service-owned bytes into the response buffer.
[POSITIVE] cells/services/vfs/src/dispatch_dirs.rs:63 — parent `CloseDir` purges file handles anchored to the full revoked directory id set before returning success, including derived cross-cell directory lineage.
[POSITIVE] cells/services/vfs/src/dir_admission.rs:23 — higher-generation contact purges predecessor-owned file handles and files anchored under revoked predecessor directories before attestation proceeds.
[POSITIVE] cells/services/vfs/src/manager.rs:163 — owner-watch bookkeeping includes `FileHandle(_)`, rolls back owned state on watch setup failure, and death cleanup purges dirs, legacy handles, pending reads, and file handles for the exact recorded generation.
[POSITIVE] cells/services/vfs/src/main.rs:123 — fast IPC remains limited to `GetFile`; `OpenFileAt` / `ReadFileHandle` / `CloseFile` have no fast arm and use only ordinary attested message IPC.
[POSITIVE] docs/specs/17-ipc-wire-contract.md:263 — Spec17 now documents the ratified request/response variants, no fast arm, no grant/raw-pointer lifetime, no hot-swap serialization, and postcard range widening to `0x19`.
[POSITIVE] docs/specs/09-vfs.md:108 — Spec09 now documents `ViVfsFileHandle` as a service-local capability, per-request reauthorization, parent/generation/close revocation, inline 4000-byte cap, and no async/grant/raw-pointer lifetime.

Reclassified false positives: prior CRITICAL on `kernel/src/task/syscall.rs:705` is not a Phase04 scope leak because kernel grant-lease/current-caller work is approved completed Phase03 dirty state; prior HIGH on `cells/tools/shell/src/cmd_fs.rs:359` is not a Phase04 scope leak because shell `Stat` + `ReadFileGrant` migration is approved completed Phase02 dirty state. The prior LOW unused re-export warning is fixed at `cells/services/vfs/src/file_handles.rs:12`.

Verification: `cargo test -p api --target x86_64-unknown-linux-gnu` passed 78 unit tests plus 2 contract tests; `cargo fmt --all --check` passed; `git diff --check` passed; `cargo build --release -p service-vfs --target riscv64gc-unknown-none-elf --features test-hooks` passed and no longer emits the file-handle unused re-export warning; `cargo test --manifest-path tests/integration/Cargo.toml --target x86_64-unknown-linux-gnu --test vfs-quota riscv64_vfs_quota_all_pass -- --nocapture` passed.

Missing proof: `cargo test -p service-vfs --target x86_64-unknown-linux-gnu` remains unusable in this checkout because the host test target pulls no-std driver code and fails with `unwinding panics are not supported without std`; this was substituted with the service-vfs RISC-V test-hooks build plus RV64 QEMU marker lane.
