**VERDICT:** BLOCKED — the slice removes raw `DataPtr` and masks service replies, but the new facade can still exhaust memory on caller-controlled reads and does not give a stable whole-file snapshot across chunks.

[HIGH]     libs/ostd/src/clients/vfs/read_file.rs:145 — `VfsClient::read_file` appends every 4000-byte chunk into one `Vec` with no total cap, so Hypha `read_file({"path": ...})` and config/key callers can force whole-file allocation before their own truncation/validation runs. Add a caller-visible max read size or a limited/streaming helper, and make consumers pass their real limits. Rollback: revert the ostd facade helper to the previous DataPtr-rejecting implementation; irreversible part: none unless a release exposes this DoS contract.
[MED]      libs/ostd/src/clients/vfs/read_file.rs:149 — the client treats each `ReadFileHandle` reply as part of one logical file, but each request is a separate VFS read and the server snapshots `read_to_vec` per call, so an interleaved write/truncate can return mixed old/new bytes or early EOF. Pin a server-side snapshot for the file handle, lock writes while a handle read is active, or document and test weak consistency before using this as the full-file facade.
[MED]      libs/ostd/src/clients/vfs/read_file.rs:29 — the new local VFS error mapper is not tied to the server constants (`ERR_QUOTA=2`, `ERR_HANDLE=4`) and the test at `libs/ostd/src/clients/vfs/read_file/tests.rs:116` expects `Err(2)` to mean `NotFound` while the implementation maps it to `OutOfMemory`. Centralize the handle-read wire codes or use named constants, then add tests for missing file, bad handle, quota, and denied.
[POSITIVE] libs/ostd/src/service.rs:117 — `ServiceRef::call` now uses `service_call_typed`, so generic service calls receive only from the resolved service tid instead of wildcard `sys_recv(0)`.
[POSITIVE] libs/ostd/src/clients/vfs/read_file.rs:82 — cleanup runs after both success and read/open errors and closes the file before unwinding directory handles in reverse order.

Verification: `cargo check -p ostd --target x86_64-unknown-linux-gnu` passed. Focused `cargo test -p ostd --target x86_64-unknown-linux-gnu read_file -- --nocapture` is blocked by the pre-existing `#[alloc_error_handler]`/`panic_impl` conflict with host `std`.
