**VERDICT:** BLOCKED — the cap ceiling design is close, but the new replacement authority is not atomically tied to a live frozen task and bypasses the existing exact launch-edge gate.

[HIGH]     kernel/src/cell/hotswap.rs:196 — `freeze_task_with_ceiling` freezes the task under `SCHEDULER`, drops that lock, then records the ceiling at lines 209-213; `ResumeCell` can run `unfreeze_task` at kernel/src/task/syscall.rs:3598 in the gap, after which the first caller still inserts a ceiling for a runnable task. Store the record in the same scheduler-guarded state, or hold a single documented lock order across both state and record mutation; rollback is to revert `FreezeCell` to `set_task_frozen` plus the supervisor `sys_spawn_from_path` call, with no irreversible state except any replacement already spawned during a bad live test.
[HIGH]     kernel/src/task/syscall.rs:3677 — `SpawnReplacement` consumes only `SWAP_CEILINGS[old_tid]` and never rechecks that `old_tid` still exists and is `TaskState::Frozen`; general death paths remove tasks via `Scheduler::exit_task` at kernel/src/task/scheduler.rs:547 without clearing `SWAP_CEILINGS`. Clear the record in `Scheduler::exit_task` or make `SpawnReplacement` atomically validate `tasks[old_tid]` is frozen before consuming; rollback is the same ABI/use-site revert, while any spawned replacement must be killed manually.
[HIGH]     kernel/src/task/syscall.rs:3679 — `SpawnReplacement` calls `loader::spawn_from_path` directly after only `SupervisorCap`, path syntax, and ceiling-record checks, bypassing `authorize_launch_edge` even though supervisor launch policy is encoded in kernel/src/loader/launch_profile/mod.rs:51 and kernel/src/loader/launch_profile/profiles.rs:58. Add a replacement launch route or call the existing supervisor profile before spawn, then intersect that ceiling with the frozen-task ceiling; rollback reverts to `sys_spawn_from_path`, with the known old cap-loss behavior returning.
[MED]      cells/services/supervisor/src/hotswap.rs:151 — supervisor hotswap still calls hard `sys_freeze_cell` before sending `Snapshot`; `ipc_send` queues messages to `TaskState::Frozen` tasks at kernel/src/task.rs:1363, so stateful cells cannot run their snapshot handler and the timeout path at cells/services/supervisor/src/hotswap.rs:161 cold-starts the replacement. This appears pre-existing, but it means the unverified QEMU E2E remains a runtime-release blocker for state-preserving hotswap claims.
[POSITIVE] libs/api/src/abi/syscall.rs:717 — `SpawnReplacement` uses a distinct allowlist bit 57, so older bit-49 supervisor allowlists do not silently gain replacement-spawn authority.
[POSITIVE] kernel/src/task/syscall.rs:3663 — the new syscall validates pointer length and UTF-8 before reading the path, preserving the existing syscall trust-boundary pattern.
[POSITIVE] kernel/src/task/syscall.rs:3687 — the RV64/RV32 error path preserves typed OOM through `supports_typed_spawn_oom`, while generic denials keep the legacy sentinel encoding.

Runtime boundary: the reported QEMU SpawnReplacement E2E gap is acceptable as a code-complete boundary only if recorded as stale-image/gen_disk-blocked evidence; it is not acceptable as a release claim that supervisor hotswap works end to end.
