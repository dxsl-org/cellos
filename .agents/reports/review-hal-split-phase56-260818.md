**VERDICT:** BLOCKED -- Phase 5/6 mostly lands the board/SoC split, but two runtime paths can pass compile checks while booting with missing or uninitialized hardware access.

[HIGH]     kernel/src/main.rs:125 -- `board-rpi4` enables `DriverId::UartPl011` at `boards/raspberry-pi/4-model-b/board.rs:8`, and `uart_pl011::putchar` requires `init()` first at `hal/arch/arm/src/aarch64/uart_pl011.rs:36`, but the only early PL011 init excludes `board-rpi4`; initialize PL011 for every active board with `UartPl011` or add the explicit rpi4 branch. Rollback: revert this hunk to the previous board-rpi4 PL011 init behavior; any serial output lost before the fix is not recoverable.
[HIGH]     kernel/src/platform.rs:326 -- real RISC-V boards with `requires_firmware_dtb=true` still fall back to hard-coded UART/PLIC/CLINT/RTC facts when a syntactically valid DTB omits a required node, so a bad DTB can drive stale MMIO instead of failing closed; for firmware-DTB boards, missing nodes for enabled drivers should panic or disable that driver before MMIO registration. Rollback: revert the fallback consumer to the last known board-owned facts while adding the fail-closed guard; MMIO writes already attempted on wrong hardware cannot be undone.
[LOW]      kernel/src/platform.rs:185 -- the QEMU ARM comment still says Goldfish RTC at `0x0902_0000`, while the implementation and tests now use PL031 at `0x0901_0000`; update the comment so future bring-up does not chase the stale address.
[POSITIVE] boards/src/descriptor.rs:86 -- `BoardDescriptor` now contains identity, boot contract, fallback memory, wiring, and enabled driver list only; SoC MMIO fields were removed from the board contract.
[POSITIVE] hal/arch/riscv/src/common/plic.rs:31 -- default PLIC base is zero and register access asserts after `set_plic_base`, which is the right fail-closed behavior for missed platform selection.
[POSITIVE] boards/qemu/virt-aarch64/board.rs:7 -- `RtcPl031` distinguishes QEMU ARM's PL031 RTC contract from RISC-V Goldfish RTC instead of overloading `RtcGoldfish`.
