**VERDICT:** BLOCKED — three production correctness/race issues remain in the HTTPD/VFS migration path despite clean format and RV64 compile gates.

[HIGH]     libs/ostd/src/clients/vfs/read_file/wire.rs:11 — Missing files still become `ViError::IO` because live `OpenFileAt` returns `ERR_IO` for `None` at `cells/services/vfs/src/dispatch_file_handles.rs:29`, while HTTPD only maps `ViError::NotFound` to 404 at `cells/services/httpd/src/handlers.rs:138`; ordinary missing static paths now return 500. Fix by making absent files use a distinct not-found code through the handle read path, then cover service and net-tools HTTPD missing-path responses. Rollback: revert the HTTPD/VfsClient migration to the prior empty-read 404 behavior; irreversible part: no persisted state, but external clients may already have observed false 500s.
[HIGH]     cells/tools/net-tools/src/bin/httpd.rs:274 — The accept loop still uses wildcard `sys_recv(0)` after sending `TcpAccept`, so a queued non-NET message can be consumed, dropped, or decoded as a bogus accept reply even though the rest of this file added NET sender masking. Fix the accept loop to use `recv_net_response(net_ep, ...)` and add a wrong-sender accept-loop test. Rollback: redeploy the previous httpd binary or stop the daemon; irreversible part: already consumed non-NET messages cannot be recovered.
[HIGH]     cells/services/httpd/src/net_ipc.rs:91 — `tcp_send_all` breaks on a zero-byte `TcpSend` progress reply, and the NET service legitimately returns `Data(0)` when the socket cannot currently send at `cells/services/net/src/handlers.rs:168`; large or backpressured responses can be silently truncated after advertising the full `Content-Length`. Fix by yielding and retrying zero progress with a bounded budget, or return a send error that closes without claiming a full body. Rollback: revert the HTTPD NET helper change or disable large static responses; irreversible part: bytes already sent on a partially delivered response cannot be unsent.
[MED]      cells/tools/net-tools/src/bin/httpd.rs:95 — Net-tools HTTPD treats an existing empty file as 404, while service HTTPD preserves `Ok(Vec::new())` as a 200 body via `cells/services/httpd/src/static_files.rs:17`; the two file servers now disagree on empty-file HTTP semantics. Fix by aligning net-tools classification with the shared static-file behavior or documenting a deliberate compatibility split with tests. 
[POSITIVE] libs/ostd/src/service.rs:117 — Shared `ServiceRef::call` now routes through sender-masked `service_call_typed`, removing the prior facade-wide wildcard reply hazard for VFS consumers.
[POSITIVE] libs/ostd/src/clients/vfs/read_file/session.rs:53 — The new handle read session closes the file and all opened directory handles through `cleanup()`, including read-error paths.
[POSITIVE] cells/services/httpd/src/static_files.rs:15 — Service HTTPD has an explicit result classifier that distinguishes empty successful bodies from missing files and maps oversize/transport failures to 500.

Verification: `git diff --check` PASS; `cargo fmt --all --check` PASS; `cargo check -p vicell-kernel --target riscv64gc-unknown-none-elf` PASS. Unit harness unavailable per prompt; QEMU HTTP host proof remains host-gated by the stale image lacking bootstrap/net.
