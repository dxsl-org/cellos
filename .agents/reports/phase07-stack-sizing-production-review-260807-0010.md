**VERDICT:** PASS - the Phase07 stack shrink is constrained to measured post-shim task names, unknown paths retain the 64-page fallback, and the changed cfg paths compile on RV64, AArch64, and x86_64.

[POSITIVE] kernel/src/task.rs:237 - `stack_pages_for` has a static six-name allowlist for 16-page stacks and leaves every unmeasured name on `STACK_PAGES` at `kernel/src/task.rs:240`; the test-hook self-test at `kernel/src/task.rs:245` proves the fallback contract.
[POSITIVE] kernel/src/task/scheduler.rs:284 - test-hook watermark priming covers both kernel and user stacks before task publication at `kernel/src/task/scheduler.rs:322`, so emitted baselines are not missing the user-stack half that drives the `vfs` peak.
[POSITIVE] kernel/src/task.rs:306 - boot baseline emission writes both `kind=kernel` and `kind=user`; `kernel/src/task.rs:321` defers `vfs-test` to exit so the historical 16-page FAT/VFS overflow concern is addressed by the full VFS-suite sample rather than an early-boot sample.
[POSITIVE] .agents/260806-1026-midori-reactor-stack-closure/reports/stack-sizing-evidence.md:7 - the report records the 2x formula, 16-page floor, and 64-page unknown fallback; the largest observed path is `vfs` user stack at 31,744 bytes on line 21, selecting exactly 16 pages with no slack arithmetic gap.
[POSITIVE] .agents/260806-1026-midori-reactor-stack-closure/reports/stack-sizing-evidence.md:30 - validation records the exact capture command, representative service/NIC/VFS/shell workload, unknown fallback proof, production RV64 gates, and three-arch boot gates after applying the table.
[POSITIVE] kernel/src/task/drivers/virtio_common.rs:38 - VirtIO-MMIO enumeration is now arch-specific: RISC-V uses DTB slots, AArch64 keeps the QEMU 32-slot scan, and PCIe-only arches return an empty iterator at `kernel/src/task/drivers/virtio_common.rs:53`, fixing the x86 boot class without ABI change.
[POSITIVE] scripts/build-test-hooks-ci.sh:86 - the test-hooks image now embeds `service-net` and `driver-virtio-net` and verifies `/bin/net` plus `/bin/virtio-net` in the FAT layout at `scripts/build-test-hooks-ci.sh:105`, reducing stale-image false evidence risk.
[POSITIVE] tests/integration/tests/boot.rs:1561 - the stack-sizing integration test waits for kernel and user markers for all six measured paths after shell commands, while `tests/integration/tests/vfs-quota.rs:80` checks the policy self-test in the full VFS quota lane.
