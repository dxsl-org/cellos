**VERDICT:** BLOCKED — missing-file classification, empty-file handling, masked `TcpAccept`, bounded handle reads, and no ABI/syscall expansion mostly check out, but service HTTPD still discards failed sends and net-tools maps one non-missing Stat case to 404.

[HIGH]     cells/services/httpd/src/router.rs:28 — Every handler now returns `bool`, and `tcp_send_all` returns `false` after bounded zero-progress retries at `cells/services/httpd/src/net_ipc.rs:65`, but the router discards the handler result with `let _ = match`; a body can still be partially sent after a full `Content-Length` and then closed with no surfaced failure. Fix by propagating/logging send failure out of `handle_connection` and treating incomplete sends as an explicit connection error. Rollback: revert the bool-return send plumbing and previous retry helper together; irreversible part: bytes already sent on a partial HTTP response cannot be unsent.
[MED]      cells/tools/net-tools/src/bin/httpd.rs:121 — `Stat { is_dir: true }` is classified as `NotFound`, but the requested split is raw VFS `Stat Err(1)` -> 404 and transport/other -> 500; service HTTPD correctly treats directories/unexpected wire replies as internal at `cells/services/httpd/src/static_files.rs:24`. Fix net-tools to map only `VfsResponse::Err(1)` to 404 and add a directory/non-file Stat test.
[POSITIVE] cells/services/httpd/src/static_files.rs:23 — Service HTTPD preflight maps raw VFS `Stat Err(1)` to 404 while transport/unexpected replies go to 500.
[POSITIVE] cells/tools/net-tools/src/bin/httpd.rs:123 — Net-tools HTTPD now maps raw VFS `Stat Err(1)` to 404 and maps transport/wire errors to internal errors at line 124.
[POSITIVE] cells/services/httpd/src/static_files.rs:30 — Existing empty files stay `Body(Vec::new())`, so service HTTPD returns 200 with `Content-Length: 0`.
[POSITIVE] cells/tools/net-tools/src/bin/httpd.rs:109 — Existing empty files stay `FileReadOutcome::Ok`, so net-tools HTTPD returns 200 with `Content-Length: 0`.
[POSITIVE] cells/tools/net-tools/src/bin/httpd.rs:319 — `TcpAccept` now receives through `recv_net_response(net_ep, ...)` instead of wildcard `sys_recv(0)`.
[POSITIVE] cells/services/httpd/src/net_ipc.rs:89 — Service HTTPD `TcpAccept` uses `service_call_typed`, which receives masked to the NET service.
[POSITIVE] libs/ostd/src/clients/vfs/read_file/session.rs:109 — VFS reads use repeated `ReadFileHandle { max <= 4000 }` chunks with cleanup at lines 53-56, and grep found no retained `ReadAsync`/`Poll`/`GetFile` fallback in the reviewed HTTPD/facade paths.
[POSITIVE] cells/services/httpd/src/main.rs:28 — HTTPD syscall allowlist remains `Send, Recv, Log, LookupService, StateRestore`; net-tools remains the same at `cells/tools/net-tools/src/bin/httpd.rs:27`, and no `libs/api` ABI/wire diff was observed.

Verification: `git diff --check` PASS; `cargo fmt --all --check` PASS; `cargo check -p vicell-kernel --target riscv64gc-unknown-none-elf` PASS. New focused files checked are under 200 lines. Unit harness remains unavailable here; QEMU HTTP host proof remains host-gated by the stale image lacking bootstrap/net.
