**VERDICT:** BLOCKED — file-handle authority checks look sound in the reviewed code, but the working tree exceeds the Law1 Phase 04 delta with kernel grant-lease/syscall lifecycle changes and caller migration work.

[CRITICAL] kernel/src/task/syscall.rs:705 — Phase 04 is approved only for `ViVfsFileHandle`, requests 23-25, response 9, and normal VFS dispatch, but the kernel now classifies registered VFS callers, turns `GrantSlice` into a VFS per-request lease path, and pins frames from the syscall layer; move this to the separately approved lifecycle checkpoint or revert `kernel/src/{task.rs,task/scheduler.rs,task/syscall.rs,task/tcb.rs,memory/pin.rs,cell/hotswap.rs,fast_ipc.rs,main.rs}` plus `kernel/src/task/vfs_lifecycle_selftest.rs`. Rollback is a code revert before release; irreversible part is none unless the broadened behavior ships and callers rely on it.
[HIGH] cells/tools/shell/src/cmd_fs.rs:359 — Phase 04 step 7 says client wrappers and caller migration remain Phase 05, but shell reads now use `Stat` plus `ReadFileGrant` and registered grants instead of the previous `GetFile`/`DataPtr` fallback; move shell/executor/shell-test/`libs/ostd` migration to Phase 05 or attach an explicit approval/evidence package for this checkpoint. Rollback is reverting `cells/tools/shell/src/{cmd_fs.rs,executor.rs,shell_test.rs,text_tools/grep/io/read.rs}`, `libs/ostd/src/clients/vfs.rs`, and the shell-utils assertions; irreversible part is none before external release.
[MED] libs/api/src/services/ipc.rs:155 — public wire variants 23-25 and response 9 are added, but `grep -RIn 'OpenFileAt|ReadFileHandle|CloseFile|FileHandle(' docs/specs docs` found no spec update even though the phase lists `docs/specs/17-ipc-wire-contract.md` and `docs/specs/09-vfs.md`; update the committed specs before landing the ABI.
[LOW] cells/services/vfs/src/file_handles.rs:12 — the test-hooks rebuild warns that `FileEntry` and `FileState` are unused under the test-hooks build; remove the unused re-export or consume it in the selftest so warning budgets stay clean.
[POSITIVE] cells/services/vfs/src/file_handles/table.rs:84 — `begin_sync_read` owner-checks the guessed handle before returning the path and leaves wrong-owner entries intact, so handle sweeping cannot read or destroy another cell's file entry.
[POSITIVE] cells/services/vfs/src/file_handles/table.rs:66 — file handle IDs advance with `checked_add` and transition to exhausted rather than wrapping or reusing closed IDs.
[POSITIVE] cells/services/vfs/src/dispatch_dirs.rs:185 — `ReadFileHandle` rechecks owner/generation via the table and then rechecks current read policy before copying bytes into the response buffer.
[POSITIVE] cells/services/vfs/src/dispatch_dirs.rs:63 — `CloseDir` purges file handles anchored to every revoked directory id, covering transitive parent revocation.
[POSITIVE] cells/services/vfs/src/manager.rs:194 — owner-death cleanup purges dirs, new file handles, legacy handles, and pending reads for the exact recorded generation.
[POSITIVE] cells/services/vfs/src/main.rs:123 — the fast IPC handler still serves only `GetFile`; all new file-handle requests fall back to normal message dispatch and return `0xFE` if attempted on the fast path.

Verification: `git diff --check` passed; `cargo test -p api --target x86_64-unknown-linux-gnu` passed 80 tests; `cargo check -p vicell-kernel` passed; `bash scripts/build-test-hooks-ci.sh` passed with warnings; `cargo test --manifest-path tests/integration/Cargo.toml --target x86_64-unknown-linux-gnu --test vfs-quota riscv64_vfs_quota_all_pass -- --nocapture` passed. `cargo test -p service-vfs --target x86_64-unknown-linux-gnu` did not run because `driver-disk` fails the host test target with `unwinding panics are not supported without std`.
