# Final QA — RISC-V PLIC Policy

## Verdict

All 11 final gates passed against uncommitted source at `HEAD c6a31372`.
The before/after working-tree file set was unchanged.

## Passing Gates

1. `cargo fmt --all -- --check`
2. `cargo test -p hal-soc-riscv --target x86_64-unknown-linux-gnu` — 3 passed
3. `cargo test -p cellos-boards --target x86_64-unknown-linux-gnu` — 8 passed
4. RV64 kernel check, default features
5. RV64 kernel check, `board-vf2`
6. RV64 kernel check, `board-pioneer`
7. RV64 kernel check, combined `board-vf2 board-pioneer`
8. AArch64 kernel check, default features
9. AArch64 kernel check, `board-rpi3`
10. RV64 release kernel build with `-Z build-std=core,alloc`
11. QEMU boot — `PASS: FAT16 mounted — kernel booted (no disk)`

## Existing Warnings

- The RPi3 compile lane retains existing `hal-arm` dead-code warnings.
- Kernel build scripts retain existing strip-tool warnings on this host.

## Evidence Boundary

QEMU runtime is verified. VF2, Pioneer, and RPi3 are compile-only; no new
physical-board runtime claim is made.
