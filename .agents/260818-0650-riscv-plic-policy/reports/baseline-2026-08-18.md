# Baseline 2026-08-18

## Verdict

The pre-change profile-slice QA passed 11/11 gates with two `hal-soc-riscv`
tests before PLIC context policy existed. The earlier workspace-wide
`cargo check --target ...` attempt was invalid because it omitted
`-p cellos-kernel` and reached unrelated hosted application build scripts; it
is not baseline evidence.

## Passing Gates

1. `cargo fmt --all -- --check`
2. `cargo test -p hal-soc-riscv --target x86_64-unknown-linux-gnu` — 2 passed
3. `cargo test -p cellos-boards --target x86_64-unknown-linux-gnu` — 8 passed
4. RV64 kernel check, default features
5. RV64 kernel check, `board-vf2`
6. RV64 kernel check, `board-pioneer`
7. RV64 kernel check, combined `board-vf2 board-pioneer`
8. AArch64 kernel check, default features
9. AArch64 kernel check, `board-rpi3`
10. RV64 release kernel build with `-Z build-std=core,alloc`
11. `scripts/qemu-boot-test.sh` — `PASS: FAT16 mounted — kernel booted (no disk)`

## Existing Warnings

- Kernel build scripts could not strip `init` and `kernel_fs.img` on this host.
- The RPi3 compile lane retained five existing `hal-arm` dead-code warnings.

## Evidence Boundary

QEMU runtime is verified. VF2, Pioneer, and RPi3 are compile-only in this
baseline; no physical-board runtime claim is made.
