**VERDICT:** PASS — the production dead-peer send path now returns a caller-visible error, the synthetic guard asserts the full wake contract, and the QEMU lane covers a real watchdog death plus queued ForceExit notification.

[POSITIVE] kernel/src/task/scheduler.rs:528 — `exit_task` now restores blocked senders to `Ready`, sets `reply_value = Some(usize::MAX)`, preserves trap `a0 = usize::MAX`, and requeues the exact sender; this matches the `Syscall::Send` resume path that reads `reply_value` after `yield_cpu`.
[POSITIVE] kernel/src/task/ipc_guardrail_selftest.rs:81 — the synthetic marker asserts `Ready`, `reply_value`, trap `a0`, and exact sender presence in a hart-local ready queue before logging PASS.
[POSITIVE] kernel/src/task.rs:1368 — a new blocking send clears stale `reply_value` before entering `TaskState::Sending`, and `kernel/src/task/ipc_guardrail_selftest.rs:107` exercises that follow-up reset after a dead-peer error.
[POSITIVE] cells/tests/bench/src/scenarios/smp.rs:45 — `heartbeat-peer` waits for a start message, arms a 5-tick heartbeat, and yields forever, forcing the parent’s second `sys_send` to resume through watchdog-driven `exit_task`.
[POSITIVE] cells/tests/bench/src/scenarios/smp.rs:84 — `run_peer_death_guard` proves caller-visible `SyscallResult::Err` for the blocked send and still drains the queued ForceExit notification path.
[POSITIVE] tests/integration/tests/hotswap-smoke.rs:119 — the final QEMU marker waits for the boot primitive guard, runs `bench peer-death-guard`, and requires `[peer-death-runtime] PASS (blocked-send error + ForceExit notification)`.

Verification: `git diff --check HEAD` passed; `cargo check -p vicell-kernel --target riscv64gc-unknown-none-elf -Z build-std=core,alloc` passed; `cargo check -p app-bench --target riscv64gc-unknown-none-elf -Z build-std=core,alloc` passed; `CARGO_BUILD_TARGET=x86_64-unknown-linux-gnu cargo test --manifest-path tests/integration/Cargo.toml --test hotswap-smoke --no-run` passed. No long QEMU rerun was performed in this review; the final QEMU PASS is taken from the re-review prompt and source/report evidence was grep-verified. `git diff -- libs/api libs/types kernel/src/task/executor.rs cells/services/vfs/src/dispatch.rs cells/services/vfs/src/main.rs` produced no output.
