# Scout Report: Codebase Docs Sync

Date: 2026-08-19
Branch: docs/codebase-sync
Base: 405384af7a481e506dd84658042c5a424bb6a0b7

## Scope

Docs-only plan for syncing codebase documentation with current source and splitting the oversized roadmap. No implementation code should be edited.

## Relevant Files And Roles

- `docs/project-roadmap.md:6` is a 1-line status accumulator; the file is 1807 lines and mixes status, history, roadmap, blockers, and release notes.
- `docs/project-changelog.md:5` records the 2026-08-19 HAL-to-kernel ABI cleanup; the file is 4794 lines and should stay chronological rather than become a roadmap.
- `docs/codebase-summary.md:16` already claims HAL-to-kernel ABI signatures are single-sourced.
- `docs/system-architecture.md:17` also claims HAL-to-kernel ABI hooks are single-sourced.
- `docs/code-standards.md:66` records the shared ABI declaration owner.
- `docs/code-metrics.generated.md:3` is generated by `scripts/generate-code-metrics.py`; do not hand-edit metrics values.

## Verified Source Facts

- `hal/traits/arch/src/kernel_abi.rs:165` declares `vi_handle_page_fault`; `hal/traits/arch/src/lib.rs:3` exposes the module and `hal/traits/arch/src/lib.rs:4` re-exports it.
- `hal/arch/x86/src/x86_64/idt.rs:203` is the only observed call site for `vi_handle_page_fault`; `kernel/src/memory/paging.rs:935` defines it and `kernel/src/memory/paging.rs:1035` type-checks it against the shared ABI type.
- Board descriptors are data objects: `boards/src/descriptor.rs:94` defines `BoardDescriptor`, `boards/src/descriptor.rs:124` implements validation, and `boards/src/lib.rs:32` through `boards/src/lib.rs:57` expose catalog accessors.
- SoC facts live below `hal/soc`: RISC-V profiles at `hal/soc/riscv/src/profile.rs:11`, x86 profile at `hal/soc/x86/src/lib.rs:42`, BCM27xx profile at `hal/soc/bcm27xx/src/profile.rs:35`, and ARM virt profile at `hal/soc/arm-virt/src/lib.rs:78`.

## Verified Open Issues To Document

- Production trust is not complete: `kernel/src/signing.rs:33` and `kernel/src/policy.rs:87` still use zero placeholder production public keys; `kernel/Cargo.toml:83` and `kernel/Cargo.toml:86` define opt-in `policy-required` and `signing-required`.
- POSIX/file-operation gaps remain: `kernel/src/task.rs:1270` notes path canonicalization TODO; `kernel/src/task.rs:1404`, `kernel/src/task.rs:1408`, `kernel/src/task.rs:1455`, and `kernel/src/task.rs:1460` expose stubbed `fstat`, `chdir`, `rename`, and `getcwd`. Callers are `kernel/src/task/syscall.rs:2658`, `kernel/src/task/syscall.rs:2666`, and `kernel/src/task/syscall.rs:2677`; `rename` reaches the FileOp stub path at `kernel/src/task/syscall.rs:3495`.
- Net-broker is partially wired: `cells/services/net-broker/src/main.rs:117`, `:118`, `:133`, `:136`, `:137`, `:138`, `:154`, `:155`, and `:156` are dispatch/key/beacon/lease/routing TODOs. Companion modules self-label unwired status at `cells/services/net-broker/src/routing.rs:2`, `cells/services/net-broker/src/beacon.rs:2`, `cells/services/net-broker/src/gossip.rs:2`, and `cells/services/net-broker/src/stun.rs:15`.
- Net service still carries a 100 ms fallback cadence: `cells/services/net/src/main.rs:66` defines `POLL_TICKS`; `cells/services/net/src/main.rs:192` sets a 100 ms wait timeout; roadmap already notes the deferred IRQ wake optimization at `docs/project-roadmap.md:1352`.
- Docs drift is real: `docs/network-api.md:194` points at non-existent `cells/services/net/src/lib.rs`, and `docs/network-api.md:199` points at non-existent `tests/integration/network_loopback.rs`. `docs/code-standards.md:426` still lists MicroPython in `runtimes/`, while `docs/codebase-summary.md:25` says only Lua remains active.

## Precedents

- `ba64c7bd refactor(hal): centralize kernel ABI signatures` changed docs, generated metrics, `hal/traits/arch/src/kernel_abi.rs`, `kernel/src/memory/paging.rs`, and `scripts/check-hal-boundaries.sh`. Blind-spot candidates for this plan: `scripts/check-hal-boundaries.sh`, `docs/code-metrics.generated.md`.
- `93aed9b4 docs(cellos): sync core docs for cell-to-cell anywhere` changed roadmap, architecture, changelog, and code standards together. Blind-spot candidate: update cross-doc references in one validation pass.
- `a7b9b235 chore(docs): restore generated metrics baseline` changed only `docs/code-metrics.generated.md`; metrics must be regenerated or restored by script, never normalized by hand.

## Prior Failures

- No `.agents/failure-history.jsonl` exists in this worktree.
- Memory-derived PRIOR warning, revalidated by current file shape: large roadmap patches are brittle; use small independent patches and validate staged file list.

## Blast Radius

- Product docs only: `docs/project-roadmap.md`, new `docs/roadmap/*.md`, `docs/codebase-summary.md`, `docs/system-architecture.md`, `docs/code-standards.md`, `docs/security-model.md`, `docs/network-api.md`, `docs/project-changelog.md`, `docs/code-metrics.generated.md`, and link/index docs if needed.
- Source files are read-only inputs for this plan.
- Validation commands should not rewrite generated artifacts unless the phase explicitly owns them.

## Inconsistencies To Resolve

- Roadmap combines current stage status, backlog, completed history, release planning, blockers, and raw dated status in one file.
- Active source says net-broker is a scaffolded foundation, while roadmap language risks reading as shipped two-node runtime.
- Runtime docs conflict around MicroPython.
- Network API references stale file paths.
- Security docs should continue separating dev/test signing mechanism from fleet-secure admission.
