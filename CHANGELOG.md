# Cellos Changelog

All notable changes to Cellos are documented here.
Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

---

## [Unreleased]

### 🚀 Improvements
- cells/service-net + net-tools: **Cellos ra internet thật, không chỉ tới host mock.** `NetRequest::Resolve` trước đây luôn trả `Err(0xFF)`; nay net cell sở hữu một smoltcp `dns::Socket` (`cells/services/net/src/dns.rs`) và trả `NetResponse::Addr` theo đúng một thứ tự: IPv4 literal → alias SLIRP (`gateway`/`host`/`dns`/`localhost`) → truy vấn A-record UDP tới DNS server **do DHCP cấp** (option 6, fallback 10.0.2.3), dùng retransmit/backoff và theo CNAME của smoltcp, trần 3 s mỗi lookup (log `[net] DNS server: <addr>`). `curl`, `wget`, `nc`, `mqtt`, Ocel và binding `vnet.*` của Lua bỏ bảng alias riêng, `parse_ipv4` riêng và DNS client cục bộ để đi qua đúng một resolver này (binding Lua hiện **chưa** được wire vào module tree của cell lua — `main.rs` không có `mod bindings_net;` — nên chưa script nào chạm tới; sửa để không còn bản sao trôi dạt khi nó được bật lại). Tầng C/POSIX cũng có tên miền: `gethostbyname` trong `libs/api/src/services/posix/net.rs` trả `struct hostent` từ chính resolver đó (static storage đúng hợp đồng POSIX, không cấp phát), và witness `posix-shim-test` chứng minh `gethostbyname("gateway")` → 10.0.2.2 rồi `connect` qua chính địa chỉ vừa resolve (`POSIX-DNS: OK`, chạy trong test `posix_shim_net` đã có trong CI allowlist; hợp đồng shim sinh lại: 201 export). Cùng chỗ đó, `send` của shim thôi đếm 20 lần yield — quan sát được là `POSIX-DNS: FAIL send ret=-1` khi handshake chưa xong — và nay bound theo đồng hồ (`GetTime` op 1, 5 s) với trần vòng lặp làm chốt chặn. Hai lỗi thời gian lộ ra khi chạy thật ra internet: vòng gửi/nhận của `curl`/`wget` bị chặn bằng *số vòng lặp* 500 nên một round trip WAN (ARP + SYN + handshake + server) tiêu hết budget trước khi request được gửi ⇒ `curl: empty response`; nay là budget theo đồng hồ (`GetTime` op 1, 30 s) cộng trần vòng lặp. Kiểm chứng: `curl http://example.com/` trong QEMU RV64 qua SLIRP trả `HTTP/1.1 200 OK` + body thật, `wget http://example.com/ /tmp/internet.txt` ghi 423 byte rồi `vcat` đọc lại được `Example Domain` (`network_reaches_the_internet_by_name`, gate theo internet của host vì SLIRP chuyển DNS/TCP của guest cho host), và `network_resolve_answers_hostname_through_the_service` (deterministic: alias `gateway` + mock host). 3 host unit test trong `cargo test -p service-net` khoá literal/alias.
- kernel/init + service_table: **budget chống crash-storm của `init` đã thực sự có hiệu lực.** `supervisor.rs` so cửa sổ 1 000 đơn vị với `sys_get_time()` = `GetTime` **op 0** (counter thô, 10 MHz `mtime` trên RV64) thay vì scheduler tick (op 4) ⇒ cửa sổ thực ~0,1 ms, roll mỗi lần exit, `restart_count` không bao giờ đạt `MAX_RESTARTS_PER_WINDOW` ⇒ service crash-loop được respawn vô hạn (Spec 12 §4.3 "≤5/~10 s rồi bỏ mặc" không có hiệu lực). Cùng lỗi đơn vị ở `READY_TIMEOUT_TICKS` của `service_table.rs` (chờ đăng ký 5 000 đơn vị thực chất 0,5 ms). Cả hai nay dùng `now_ticks()` — op 4, đúng đồng hồ kernel dùng cho deadline `RecvTimeout`. Kiểm chứng bằng witness mới: `bench init-giveup` đập `/bin/config` 6 lần và đòi `init` bỏ mặc nó; `init_gives_up_after_a_crash_storm` nằm trong boot suite + CI allowlist — PASS với fix (~12 s), còn khi stash fix thì `kill 6/6` rồi `restart 6` và FAIL đúng như bug.
- kernel/argv + ostd/shell: **command line của cell không còn bị mất.** Hai lỗi độc lập trên cùng một đường: (1) `StagedSpawnArgvCleanup` được dựng bằng `bool::then_some(...)` — `then_some` nhận tham số *theo giá trị*, nên guard tồn tại ở **mọi** syscall và bị drop ngay khi predicate sai, và `Drop` của nó xoá argv vừa stage ⇒ bất kỳ syscall nào chen giữa `set_spawn_argv` và spawn (chính là VFS-read bên trong `sys_spawn_from_path`) đều xoá sạch: `httpd 9091 /tmp/resp.txt` nghe ở `:8080`, `tier2-exploit peer` chạy mode NULL, `bench peer-death-guard` chạy suite mặc định; (2) attempt bị **từ chối** cũng xoá argv theo thiết kế, nên khi shell fallback từ route Elf (bị từ chối với target mang quyền như `bench`) sang route Path thì command line đã mất — shell nay thử `sys_spawn_from_path_raw` trước và **publish lại** argv trước khi fallback (chỉ caller làm được: `set_spawn_argv` là slot của chính nó, `sys_spawn_args` là one-shot). Kèm theo `cells/tests/posix-shim-test` bổ sung `GetRandom` vào allowlist (cell lắp cho fstat smoke, các smoke entropy/socket thêm sau bị deny). Kiểm chứng local: `hotswap-smoke` 15/15, `tier2-fault-isolation` 5/5, boot `network_*` 7/7 (một case wget flaky), `mqtt_*` 2/2, `posix_shim_*` 3/3.
- ostd/actor: **ứng dụng tự khai báo supervision tree, không cần sửa `/bin/init`.** `ostd::actor` là vòng mailbox actor trên envelope `0xAC` sẵn có — dispatch postcard có kiểu, `call`/`reply` recv **masked theo peer tid** (Spec 17 §2), lifecycle `on_start`/`on_tick`/`on_shutdown`, và log to cho message không decode được (§7, không silent drop). `ostd::actor::supervisor` thêm bảng con **động** với `Policy` (Permanent/Transient/Temporary), intensity theo cửa sổ (mặc định ≤5 restart/~10 s, kiểm tra *trước* khi tăng — đúng thứ tự của `init`), **give-up chỉ với đứa con đó**, backoff mũ có trần, và `Strategy` one_for_one/one_for_all/rest_for_one. Không đổi `libs/api`, không thêm syscall/opcode/discriminant: message actor nằm trong envelope `0xAC 0x00` nên `Shutdown`/`CapRevoked`/hotswap vẫn về cùng mailbox. ADR-0021 chốt thiết kế (monitor vẫn gate `SpawnCap`, namespace vẫn service id `u16`). 8 host test cho quyết định thuần (policy/intensity/backoff/strategy); `ostd` nay bật được harness host theo tiền lệ `libs/api` (`#![cfg_attr(not(test), no_std)]`).
- cells/backend + kernel/launch_profile: **witness B0 chạy thật trên RV64 QEMU** (`scripts/qemu-actor-supervisor.sh`): một supervisor spawn 3 worker, ping bằng typed call, tự `ForceExit` một worker → restart trong **≤100 tick (<1 s)** kèm exit reason quan sát được, rồi 6 exit bất thường trong một cửa sổ → give-up đúng đứa đó trong khi 2 worker còn lại vẫn trả lời, và một tree `one_for_all` 2 con chứng minh cả kẻ lỗi lẫn anh em được dựng lại. Launch edge là một dòng exact `backend-supervisor → /bin/backend-worker` (`Path|Elf`, ceiling rỗng, `requires_lifecycle_authority=true`); `/bin/backend-supervisor` phải nằm trong VIFS1 vì kernel từ chối ceiling khác rỗng trên route `SpawnFromElf` (bytes do caller cấp) — cùng chỗ `hotswap-demo` đang nằm.
- kernel/revocation: **runtime capability revocation is real now, not a label change.** `sys_cap_revoke` used to clear TCB fields and nothing else — every authority already handed out stayed live, which is exactly the stale-authority retention `spec/16` cites the J-Kernel proof for (LBI prevents forgery, not revocation). Revoking an `mmio_devices` bit now releases the window in the resource registry and takes its *user* accessibility away (`paging::revoke_mmio_user`: x86 clears the user PTE, riscv64/aarch64 lower the permission with `protect_page` so the frame stays identity-mapped for the kernel), reclaims the target's owned grants (a revoked device class can be the authority behind a grant, and grants carry no source-cap tag), and then clears the field — teardown first, so a teardown that cannot finish returns `NotSupported` with the capability intact. Verified on RV64 QEMU: `MMIO-REVOKE-CLASS`, `MMIO-REVOKE-ALLOWLIST`, `MMIO-REVOKE-USERBIT`.
- kernel/iommu: `iommu::unmap_dma` is no longer a no-op stub. `Sv39IommuPt`/`VtdSlpt` gained a lookup-only `unmap_range` (missing child tables are skipped, never allocated; intermediate tables are never freed, so a sibling range keeps its mapping), the x86 leg zeroes the SLPT leaves and drains the domain's IOTLB, and the riscv leg acknowledges the batch with one IOTINVAL.VMA + IOFENCE. `iommu::cleanup_cell` became `iommu::revoke_dma_for_cell`: one teardown, reached from cell death *and* from a runtime revoke, so a revoked driver Cell ends in the hardware state it would reach by dying. Verified: `IOMMU-TEARDOWN-LEAF/IDEMPOTENT/LOOKUP-ONLY/REMAPPABLE`.
- kernel/grants: a Cell's owned grants can be reclaimed without waiting for it to die — `reclaim_owned_grants(tid)` is the reaper's owner-side pass, extracted so death and revoke share one implementation, and it routes every frame through the same pin transaction as death: a region an in-flight operation still holds is **quarantined, not freed** (D36), and returned when the driver acknowledges. Grants the target merely *received* are untouched — they belong to another owner's authority. Verified: `GRANT-RECLAIM-OWNED/RECEIVED/PINNED`.
- kernel/revocation: `pcie_driver`, `platform` and `supervisor` are revocable end to end. Three additive `cap_mask` bits (`PCIE_DRIVER = 1<<4`, `PLATFORM = 1<<5`, `SUPERVISOR = 1<<6`; Law-1 confirmed twice) dispatch their eager teardown: `pcie_driver` drains the whole DMA domain plus its requester entries, releases its BDFs and its BAR windows and reclaims its grants; `platform` releases the ECAM config-space window; `supervisor` is a field clear (kernel-minted, never propagated, no ambient resource behind it). `task::teardown_dma_authority` is shared with the retirement path. Verified: `REVOKE-PRIVILEGED` inside `thread-cap self-test PASS`.
- kernel/ipc + ostd: a victim is told what it lost. The kernel sends `[0xAC, 0xF2, mask_le4]` fire-and-forget after the teardown, `spec/17` §3 registers `0xF2` as `CapRevoked` under the `0xAC` namespace, and `ostd` decodes it into `AppEvent::CapRevoked { mask }` (additive; cells already carry a wildcard arm). Delivery is best-effort and never fails the revoke — the teardown is what enforces it. Host tests pin the wire contract and the truncated-envelope case.
- port-platform/kernel: a narrow C child-launch adapter (`libs/port-platform/{include/,}cellos_spawn.{h,c}`) publishes `cellos_spawn`, `cellos_child_wait`, `cellos_spawn_argv_raw`, `cellos_spawn_argv_split`, và `cellos_spawn_receive_grants`, ghép đúng một reviewed launch edge (`(caller, route, target)`, ceil CapSet rỗng) với command line đã stage và các grant endpoint tường minh qua `PipeShare` — handle không bao giờ đi trong argv. Witness Tier-2 `cells/tests/c-spawn` + `c-spawn-child` (`scripts/qemu-c-spawn.sh`) chứng minh trên RV64 QEMU harts 1 và 2: admission Tier 2 cho cả hai cell, argv 2 item tới byte-exact, payload 192 byte có thứ tự + EOF chính xác, status con lấy được cả từ `Wait` lẫn từ endpoint, target không review bị từ chối, command line quá dài bị chặn trước kernel, và một lần launch bị từ chối không để lại command line nào.
- kernel/argv: command line đã stage không còn nằm trong map khoá `tid` mà ở hai trường task-local (`Task::staged_argv` cho lần launch kế tiếp của chính task, `Task::inherited_argv` cho con được spawner trao trước khi chạy). Map cũ cho phép cleanup "lần launch thất bại" của một task *khác* xoá argv đang chờ của con — tái hiện được: `install tid=21 len=24` rồi `take tid=21 -> None` cùng địa chỉ virt+phys, không remover nào được ghi nhận, và con báo `argv items=0`; nay lớp lỗi đó bất khả thi theo cấu trúc. Hành vi quan sát được của `StateStash`/`StateRestore` trên key argv giữ nguyên.
- kernel/loader: `SpawnFromPath` bị từ chối nay ghi một dòng WARN nêu caller, path và lý do (`NotFound`/`PermissionDenied`/…), thay vì chỉ trả `-1` cho cell; đây là thứ chỉ ra rằng route path thô không giải được cell trên đĩa vì kernel cố ý không điều khiển block device trên QEMU.
- port-platform/pthread: `cellos_pthread.c` dùng chung thunk syscall trong header nội bộ mới `cellos_syscall.h` thay vì giữ bản sao riêng, để adapter spawn và pthread có đúng một định nghĩa hợp đồng thanh ghi.

- cells/service-httpd: support CLI arguments `<port> [vfs_path]` while retaining default port 8080 and REST/AI routing when run without arguments. `cells/services/httpd/src/router.rs` serves the requested VFS file directly on connection when a path is supplied, unifying the legacy tool use case (`httpd 9091 /tmp/resp.txt &`) with the web server use case (`httpd &`) and resolving `network_httpd_serves_file` and `network_httpd_dynamic_content` in the Network Data-Path suite.
- c2c-oracle: sign isolated cell binaries in `scripts/run-c2c-broker-oracle-qemu.sh` using `lib-sign-cells.sh`. Under ADR-0015, unsigned cells are admitted to Tier 2 Paged Domains whose private `satp` roots lack peripheral MMIO mappings; signing them with the dev key ensures driver cells (`platform`, `block`, `vfs`) are admitted as Tier 1 Trusted SAS cells with full MMIO/DMA access, completely passing the C2C Broker Oracle test.
- cells/service-hypervisor: declare and initialize a 10 MiB custom heap via `ostd::declare_custom_heap!` in `cells/services/hypervisor/src/main.rs`. When `HV_VOLATILE_DISK=1` is set, `virtio_blk.rs` allocates a 4 MiB volatile disk buffer which previously exceeded the default 1 MiB heap, causing `OOM: cell heap exhausted — size=4194304` in an infinite restart loop during x86_64 hypervisor smoke tests.
- cells/tetris-c: the Tetris-OS port no longer depends on an unfetchable gitlink. Its upstream source (Banaxi-Tech/Tetris-OS, MIT, commit `66c4466`) is vendored into `cells/demos/tetris-c/src/c/tetris-os/` with its licence, `build.rs` records the provenance, and the one change we carry — the idle loop's per-ISA wait instruction (`hlt` on x86, `wfi` on RISC-V, because upstream's x86 form does not assemble for riscv64) — is tracked instead of sitting as uncommitted dirt in a nested clone. `gen_disk.ps1` now probes the real path (`src/tetris.c`), so the cell is packaged rather than silently skipped, and the platform shim gained the two hooks upstream actually calls (`keyboard_set_layout`, `speaker_music_update`) so `cargo build -p tetris-c --target x86_64-unknown-none` links end to end. The riscv64 link still needs the xpack picolibc, which is why the CI exclusions keep the cell and their comments now say so.
- kernel/rv64: the trap boundary now holds the root invariant the intermittent `/srv` fault exposed. Trap entry parks the interrupted `satp` in the trap frame — RV64-only, 304 bytes including padding, so the x86_64/aarch64 layouts and their 288-byte frames are untouched — and installs the kernel root from the fixed-name `VI_KERNEL_SATP` symbol before any kernel code runs, so the timer tick's console poll and the PLIC claim never issue an MMIO access under a Cell root. `__trap_exit` restores the frame's root only when the live value differs (a task resumed through a context switch already has it; a primed first-entry frame carries 0), and `SwitchPlan::SameDomain` now programs its domain's root instead of the zero no-write sentinel — leaving it at `(0, 0)` would resume a Cell under the kernel root with the kernel's mappings visible to its S-mode code. Private-domain user copies already branch on the resident root, so they take their stricter PTE-walk alias path inside a handler, and `validate_kernel_range` now walks the kernel root and refuses user addresses passed as kernel buffers. Evidence: 10/10 consecutive clean boots on the phase-03 direct-drive instrument (`posix-shim-test` reaching `POSIX-RENAME: OK`, zero `[KERNEL PANIC]`), 10/10 green `cargo test --test srv-cellosfs` runs, and the RV64 domain suite green on one and two harts with the new `S22-RV64-RESUME-ROOT` fixture plus a `resume-root` case in `scripts/qemu-native-domain-test.sh`. Hosted: the `CellosFS /srv Integration Test` job is green on run 34933281918 (its first green push); the `C2C Broker Oracle` job's boot no longer prints the 487 PLIC-fault kernel exceptions that used to halt it (923 on a local control) — it now reaches Embedded Init and fails on a cell-side fault, which is the C2C lane's own remaining defect; and the `Network Data-Path Integration (riscv64)` suite goes from 4 panicking failures among 6 to 2 failures with zero kernel exceptions (`52 passed; 2 failed`), the remainder being the two HTTP-server tests that failed without a panic before the fix too. `.agents/260914-ci-gate-restoration/phase-04-trap-root-discipline.md`.
- rv64/test-runner: `scripts/qemu-native-domain-test.sh` no longer pins the `SMP-FAULT-RETIREMENT` canary to `generation 99`. That field is `NEXT_DOMAIN` — how many private AddressSpaces had been built when the synthetic trap published its record — and the unmodified kernel reads 133 on this workstation today against 99 in the phase-07 captures, so the pin failed a correct kernel at `--harts 2` (measured on both the pre-change and post-change builds). The canary is now matched by cell, task, injected cause, and null pc/addr, with the measurement recorded in the comment.
- vfs/srv: the RedoxFS retirement is complete end to end. P5 ships raw and the guest formats it as CellosFS Native (no host `redoxfs-ar` build in either image script), the suite, its CI job, its cache key and its labels are `srv-cellosfs`, ADR 0016 supersedes ADR 09b and marks ADR 0002's backend as historical while its G1/G2 policy survives, and the remaining RedoxFS labels in kernel/VFS comments plus the orphaned `backend_stub.rs` are gone. Nothing in the workspace depends on `third_party/redoxfs`; it stays vendored for reference. The job's red state at that point was the kernel trap-root fault, not the filesystem — closed by the trap-boundary fix above.
- kernel/rv64: the kernel-fault panic prints the live `satp` beside the kernel's own, and the kernel records that root when it activates its page table. This is the instrument that proved the intermittent `console_drv::poll` fault (UART line-status load at `0x10000005`, `scause=13`) runs with a private Cell root live — `satp` ASID 1 — instead of the kernel root's ASID 0, because the trap entry never restores the kernel root. `hal/arch/riscv/src/rv64/{trap,domain,paging}.rs`; the fix is designed in `.agents/260914-ci-gate-restoration/phase-04-trap-root-discipline.md`.
- ai/rpi3: the inference stack runs on the physical Raspberry Pi 3 over the existing static-TFTP netboot lane. The fixture's golden ids and embedding reproduce on real Cortex-A53 (`ai-test` PASS, 8 tokens in 59 ms), and `tools/rpi3-netboot/serve-ai-oracle.ps1` boots a chosen payload and captures the board's console. The checkpoint no longer has to live inside the kernel: `service-ai` reads `/bin/ai-model.gguf` and then `/mnt/sd/ai-model.gguf` (the card's own FAT volume) and logs the path it used, and `build-aarch64-cells.ps1 -AiCells` builds the AI cells with no checkpoint at all — the 43.6 MB embedded-payload variant panics the board's kernel at compositor setup while the same image passes on QEMU aarch64 virt.
- ai/oracle: the inference oracle now runs on **both** ISA legs of Spec 24 CP-3's gate. `scripts/run-ai-inference-oracle-qemu.sh --arch riscv64|aarch64` builds, signs, and boots the same isolated image on either architecture, the CI job is a two-leg matrix, and each evidence artifact names the architecture and model it ran against. On aarch64 (a softfloat target, so its float arithmetic is software) the fixture's golden ids and embedding reproduce exactly, and a real 26.7 MB checkpoint generates coherent prose — a numerics cross-check of the integer Q8_0 kernels on a second ISA, not just a second boot test.
- ai-engine/tensor-math: **4.3× faster inference in the Cell, 1.8× on the host.** Activations are now quantized to Q8_0 once per projection and each 32-weight block is an exact integer dot product scaled by `d_w·d_a`, instead of decoding every block to f32 and running 32 f32 multiply-accumulates. Measured: 24 tokens in 1595 ms in the QEMU RV64 cell (was 6.85 s), 23.6 ms/token on a 135M-parameter Q8_0 checkpoint on the host (was 41.6 ms; 211.5 ms before the profile slice), and the per-kernel rate goes 6.8 → 12.5 GFLOP/s — faster than the *dense* f32 kernel in the same crate. The integer sum is exact in `i32` (four orders of magnitude of headroom), so the only error is the activation's half-step quantization; the golden reference in `scripts/gen-ai-test-model.py` now mirrors the shipped arithmetic and still produces the same eight token ids.
- ai-engine/tensor-math: the activation quantizer (`quant::q8_0_row_from_f32`) and its f16 encoder (`quant::f32_to_f16`, round-to-nearest-even, checked against all 65 536 half patterns) are the new public kernels; `matvec_q8_0` stays as the f32 reference the integer kernel is bounded against and as the benchmark's comparison row.
- ai-engine/tensor-math: the inference engine now builds at `-O2` instead of the workspace's size default. The Q8_0 matvec kernel — which is the entire decode path — was spending its time in out-of-line helper calls per 32-weight block. Measured: **5.1× on the host** (135M-parameter Q8_0 checkpoint 4.7 → 24.0 tokens/s; `stories15M` 24.1 → 4.7 ms/token) and **13% faster in the QEMU RV64 cell** (24 tokens in 6.85 s vs 7.89 s), with the `service-ai` image 6 KB smaller. `-O3` was measured too: faster on the host, 11% slower in the cell (TCG softfloat dominates), so it was rejected.
- ai-engine: host benchmark `cargo bench -p ai-engine --target x86_64-unknown-linux-gnu --bench cpu_engine` reports load, prompt-processing, per-token decode rate, and per-kernel GFLOP/s at the model's own shapes — the instrument behind those numbers, never asserted in CI.
- ai-test: the in-cell oracle prints its own generation timing (`[ai-test] generate: N tokens in X ms`), so cell-side inference speed is visible on every oracle run.
- hypha: `llm-gateway` now asks the on-device inference Cell first (`/bin/ai`, Spec 24 `service::AI`) and names the backend and model it used per turn; the OpenAI-compatible network endpoint stays as the fallback for images with no local model, and only absence (`NoService`/`NoModel`) or a prompt above `ai_proto::MAX_PROMPT_BYTES` reaches it.
- ai/httpd: expose `POST /api/infer` as a bounded JSON consumer of the frozen `AiClient`; canonical RV64 hostfwd QEMU coverage proves prompt → inference Cell → response, `max_tokens`, and caller-error handling.
- httpd/build: wait for `Content-Length` bodies across TCP segments, reject prompts over the AI wire limit, and build/sign the current `service-httpd` binary into `gen_disk.ps1` instead of packaging a stale `/bin/httpd`.
- vfs: introduce CellosFS Native pure-Rust CoW extent engine (`libs/cellos-fs`), replacing external RedoxFS and LittleFS dependencies with power-loss-resilient dual-ring superblocks and vector block DMA
- bench: implement native stateful workload scenario (1,000 ops, checkpoints, v1->v2 hotswap, and VFS restart recovery) verified in QEMU
- robot: implement LAB-01 carrier transfer (06B), BASE-01 tray handoff (07B), and ASSEMBLY-01 stationary coupling (08A/08B) QEMU native witnesses with real CellosFS Native trace logging
- lab: add a bounded model-only LAB-01 carrier-transfer contract with independent host fault plants
- base: add a bounded model-only secured-tray handoff contract and independent host fault plants
- performance: add immutable profile-bound RV64 benchmark captures with separate validity, target, and regression verdicts
- platform: enumerate checked inclusive MCFG ECAM ranges across buses with canonical BDFs and per-bus VT-d contexts
- x86_64: install 256 vector/CPL-aware IDT entries and verify them with an isolated `x86-idt-cpl3-test` two-task Ring-3 QEMU oracle
- security: land non-admissible Tier 1 admission catalog/test infrastructure; Phase 04 production evidence remains blocked
- kernel: enforce post-relocation W^X page permissions
- signing: gate cells through F1/F5 admission
- ipc: add bounded per-cell completion queues
- vfs: add inherited directory capabilities
- security: attest caller identity across VFS IPC
- hypha: native AI agent Cell, LLM gateway and chat loop
- net: TLS server certificate verification via embedded-tls
- ostd: http-core crate with HTTP/1.1 and JSON client
- disk: package app-https-demo as /bin/https-demo binary
- security: signed operator policy with Ed25519 in-kernel verify
- security: spawn-time capability intersection and delegation
- shell: Phase 17 parser, pipes, redirects, history, aliases
- net: smoltcp 0.11 Cell with DHCP and VirtIO NIC driver
- compositor: software blending, z-order, damage, 30 FPS
- input: US QWERTY keymap, modifier tracking, focus dispatch
- scripting: Lua 5.4 multi-line REPL with VFS bindings
- scripting: MicroPython v1.24.1 for RISC-V bare-metal
- hot-migration: ViStateTransfer, HotSwap syscall, grant chains
- bench: /bin/bench with 4 scenarios and perf CI integration
- community: CODE_OF_CONDUCT.md and contributor dev tooling
- doom: Freedoom Phase 1 port, boots and renders first frame
- vfs: add OP_MKDIR, OP_RMDIR, OP_UNLINK to IPC protocol
- kernel: add TryRecv, NetTx, NetRx, StateStash, StateRestore syscalls
- performance: release builds for all bootstrap table entries

### 🐛 Fixes
- ci/host-tests: **474 host test đã pass nhưng không job nào chạy chúng nay đã vào CI.** Sau khi hai host harness `cellos-kernel`/`ostd` được bật lại, một lượt quét `cargo test -p <crate> --target x86_64-unknown-linux-gnu` trên các crate thư viện cho thấy nhiều suite thuần logic (không đụng môi trường host: không temp file, đồng hồ, socket, RNG — đã kiểm) chỉ thiếu một dòng trong job host unit tests: `ai-tokenizer` 27, `attestation` 14, `authority-protocol` 25, `cellos-fs` 12, `http-core` 52, `gguf-rs` 20, `viui` 46, `service-kms` 59, `service-net` 35, `cellos-boards` 12, `app-wasm` 3, `service-silo` 17, `driver-dwc2-usb` 12, `driver-bcm-display` 11, và `service-net-broker` **114** (crate này ghi rõ trong `lib.rs` là phải chạy `--lib`, vì các module trong `main.rs` chỉ dành cho bare metal). Tổng **474 test**, chạy 3 lượt liên tiếp ổn định, tất cả PASS ở local rồi được thêm vào `.github/workflows/ci.yml`. Phát hiện kèm theo, ghi ở `.agents/TODO.md`: còn ~230 `#[test]` trong `cells/` vẫn không chạy ở đâu (crate bin với dep bare-metal, ví dụ `service-vfs` cần `driver-disk` host-build được; crate này cũng chỉ có đúng 2 cầu selftest chạy trong guest). Sửa kèm: `docs/code-metrics.generated.md` regenerate (kernel nLOC 38 731 → 38 748 vì test inline trong `kernel/src/task/syscall.rs`).
- kernel + libs/ostd: **hai host test harness không thể chạy, nên mọi pin trong đó chỉ là văn xuôi.** `cargo test -p cellos-kernel --target x86_64-unknown-linux-gnu` fail ngay ở compile — `kernel/src/resource_registry_tests.rs` import `static_range_allowed`, một hàm đã đổi thành `static_range_class(...) -> Option<u8>` — và một import mục như thế đủ để cả harness không build, kéo theo toàn bộ pin launch-profile/resource-registry/syscall-arg/user-copy không ai chạy (đúng ghi nhận "chỉ là tài liệu" trong `.agents/TODO.md`). Nay harness build được và **111 test PASS** (5 lần liên tiếp, và `--test-threads=1`); ca `rejected_external_spawn_discards_staged_argv` được thay bằng hai pin đúng hợp đồng hiện tại — staged argv **sống sót mọi syscall không phải launch** (đúng lỗi `then_some` đã sửa) và **bị xoá bởi một launch attempt kể cả khi attempt đó bị từ chối** — cộng pin "stage cho task không có record sống thì lưu 0 byte" (lý do staging là task-local). `libs/ostd` cũng chạy lại được: 6 test chunking `read_file::tests::bounds::*` đã lệch khỏi session (chúng có trước bước `StatAt` mà `file_size_capacity` thêm vào để reserve buffer theo size của file), nay mock có `MockReply::Stat`, chỉ số call được cập nhật theo thứ tự thật, và thêm hai pin cho chính hành vi mới (buffer reserve đúng bằng size file; fallback `min(max_bytes, MAX_READ_CHUNK)` khi backend không trả stat) — **36 + 5 + 19 test PASS**. Cả hai vào job host unit tests trong `ci.yml` (`cargo test -p cellos-kernel|ostd --target x86_64-unknown-linux-gnu`) để drift không lặp lại.
- cells/service-net + c2c-oracle: **gate `idle_ipc_wake` không còn phụ thuộc tốc độ runner.** Classifier cũ so `elapsed_ticks` — *lúc drain được IPC* — với trần `900000` (9 khoảng tick *danh định*, tức 90 ms). Nhưng tài liệu của chính chương trình đã ghi rằng một drain **không** quy được nguyên nhân cho lần trả recordless trước nó, và số đo xác nhận `elapsed` là *nhịp của caller*, không phải độ trễ đánh thức: 26 quan sát local có burn 435 709–1 136 235 tick với **1** lần dưới trần, còn trên runner cả 71 vòng đều ≥ 1,2 M tick trong khi khoảng tick thực đo được ở đó là ~1,2–1,4 M (local ~1,03–1,14 M) — tức băng PASS 90 ms hẹp hơn nhịp caller của *mọi* host chậm, nên run đúng vẫn đỏ. `idle-ipc-wake-oracle` nay đo đúng đại lượng mà trần muốn phân biệt: độ burn của chính lần wait (`return_ticks`, đọc ngay lúc trả recordless, in trên cả dòng `ARMED` lẫn dòng kết quả), kèm `sender` của IPC đã drain; verdict của harness dựa trên burn đó, bắt mọi quan sát well-formed (cùng cycle, `raw_ret=0`, `wake=recordless`, budget/ceiling khớp, `elapsed >= return`), bắt drain theo sau trong `elapsed - return < trần`, và chấp nhận một population toàn deadline (`verdict=quantum-spanning-idle-out`, in kèm số đo) vì cơ chế — park bị từ chối khi đã có IPC xếp hàng, và wait đang park được đánh thức bởi enqueue — do selftest kernel `IPC-PENDING`/`NET-RX-RESERVATION` chứng minh, đúng những marker mà test này vẫn bắt buộc. Kiểm chứng: `cargo test --test c2c-broker-oracle runtime_wake_verdict_tests` **5/5** (3 case bị từ chối: PASS đúng ở trần, drain đứng sau trần, run không có recordless return nào); `scripts/run-c2c-broker-oracle-qemu.sh` local PASS với `verdict=sub-ceiling-wake armed=26 closed=2 passes=2 min_return_ticks=614697 max_drain_gap_ticks=4899`.
- cells/unsafe-allowlist: the three C port-platform witnesses (`cells/tests/c-pthread`, `c-spawn`, `c-spawn-child`) were committed without F1/F5 exemptions, so the signing gate refused with `REFUSED: not signing — the F1/F5 check above did not pass` and **every** test-hooks lane stopped building (`build-test-hooks-ci.sh` → `qemu-native-domain-test.sh`, the aarch64/x86 hooks lanes). Each file's `unsafe extern "C"` block is the only way to call its linked C witness, so they are allowlisted as `c-ffi` (three `[[file]]` entries plus the matching `[[crate]]` entries). Verified: the gate now reports `OK: signed 8 cell(s) with the dev key after a passing F1/F5 check`.
- kernel/audit: the audit ring no longer loses records when two harts log at once. `log_event` read `head`, wrote its bytes, and stored the advanced `head` without any cross-hart exclusion — the module doc said the interrupt guard was "safe on single-hart" — so two harts could claim the same byte range and both store the same advanced cursor, silently overwriting one record; the ring carries security events (`CellSpawnDenied`, `CellSignatureFailed`, `SyscallDenied`). S-mode interrupts stay disabled (which also keeps the new lock unreachable from an ISR, so it cannot self-deadlock) and a `WRITE` lock now serialises producers, so a record is written before its cursor is published and `head` always advances by whole records.
- kernel/atomic-publication tests: `AP-13` reported a missing observation as a contract failure. The fixture asks the RT hart to inspect ready-queue visibility from inside a scheduler entry while the publisher owns `SCHEDULER`; when that hart did not reach the probe inside the bounded spin budget the observation never happened, and the case failed — measured on `--harts 2` as FAIL/PASS/PASS with one build, and FAIL again on 2026-09-18 before this program's kernel changes. A probe build showed every other conjunct true (`task/next_id/ready/quota/measurements/evidence`), i.e. the publication itself was correct in every failing run. `competing_hart_schedule_attempt` now returns `Some(true)`/`Some(false)`/`None`, and `None` logs `ATOMIC_PUBLICATION_AP-13: SKIP (competing hart did not reach a scheduler entry)` instead of failing; an observed early visibility is still a hard FAIL. The same change drops the `audit_delta.is_multiple_of(18)` rule, which cannot hold on two harts because the ring interleaves records of different lengths (the probe measured a 54-byte delta = three records) — the rule is now "at least two records' worth of evidence and nothing dropped while publishing", with `AP-15` keeping its exact `== 36`.

- scripts/qemu-futex-test: the futex witness runner no longer reports a missing PASS marker for a cell that is still working. The witness needs ~68 s from the command to its marker on this workstation (10 000 serialised futex increments plus 2 000 two-thread ping-pong rounds) while the runner waited 45 s inside a 90 s QEMU window — measured: 4 of 6 runs reported `FAIL: futex-test did not reach its PASS marker` with the cell healthy, and a long-window run reaches PASS in 68 s with every phase correct. The boot and marker windows are now 180 s and 150 s, both env-overridable (`BOOT_WINDOW`, `MARKER_WINDOW`). Verified: `FUTEX-TEST-QEMU: PASS` on harts 1 twice and harts 2 once.

- cells/futex-test: the phase-04 futex witness builds again. `fail()` was declared `-> !` while its body only recorded the failure and printed — and the arms that call it sit beside `()` arms (`Ok(0) => println!(…)`), so the declaration, not the body, was wrong; `cargo build -p app-futex-test` died with `E0308: expected !, found ()` and `scripts/qemu-futex-test.sh` could not run at all. The helper now returns, which is what its `FAILURES` counter and the tail's `sys_exit(1)` already assumed. Verified: `FUTEX-TEST-QEMU: PASS` on harts 1 and 2.

- kernel/launch_profile: restore two reviewed shell launch edges that an in-flight edit had dropped. `/bin/cpp-smoke` is phase 02's own deliverable (`cpp-freestanding`) and `/bin/curl` was present in HEAD; both were missing from `reviewed_user_target_ceiling`, so `scripts/qemu-cpp-smoke.sh` died at `DENY launch edge: caller=19 name=shell route=Elf target=/bin/cpp-smoke` followed by `shell: command not found: cpp-smoke`, and `curl` was unlaunchable. Both entries are back (curl in its original position), `/bin/cpp-smoke` is pinned in `launch_profile::tests::shell_reviewed_capability_free_edges_remain_empty` so the same silent drop cannot recur, and `scripts/qemu-native-domain-test.sh` is executable like every other runner instead of mode 644 despite being cited as a command by phase 01. Verified: `CPP-SMOKE-QEMU: PASS` again.

- rpi3/input: the lock keys now mean what they say. Caps Lock shifts the alphabetic keys only — it used to shift the digits and punctuation too, which is Shift's job, so `1` became `!` with Caps Lock on — and Num Lock chooses the keypad's face instead of leaving every keypad key hardwired: with Num Lock on `KEY_KP7` types `7`, with it off it is still Home, keypad `/` and `=` are no longer dropped as unknown, and keypad 5 is inert with Num Lock off rather than mapped onto a neighbour that would move a cursor. The keyboard's own LEDs follow the locks again: the input service hands its lock bitmap to every registered producer on each change and once when a producer registers, and the HID driver writes it back with `SET_REPORT` as the device's own LED output report — the one its report descriptor declares, or the fixed boot byte for a boot-capable interface that declares none.
- rpi3/usb-hid: lock LED updates are now reliable on the LAN9514 split-transaction path. A transient NAK/NYET no longer drops the CAPS/NUM/Scroll bitmap: each HID interface retains only the newest requested state and retries its descriptor-defined `SET_REPORT(Output)` after input polling until accepted. Physically verified on Raspberry Pi 3: each CAPS or NUM press changes both logical state and its LED.
- rpi3/usb-hid: restore physical USB keyboard input through the LAN9514 transaction translator. DWC2 DMA polling now waits for `CHHLTD` before classifying handshakes, interrupt-IN complete-splits follow the accepted start-split immediately within U-Boot's four-microframe budget, active host-channel aborts use the DWC2-required `CHDIS|CHENA` request and wait for `CHENA` to clear, and USB HID source registration retries unless `sys_try_send` returns the actual delivery result `Ok(0)`. The input layout also maps `KEY_KPENTER` to Return. Verified on a Raspberry Pi 3 with receiver `2a7a:8a53`: `a` reaches the shell, keypad Enter submits it, and `ls` completes on the cleaned production image.
- hypha: restore `/bin/hypha` launchability. The reviewed `(shell, Path, /bin/hypha)` launch edge carries `spawn`, but the shell's VFS+grant spawn takes the ELF route, which `launch_profile::authorize` refuses for capability-bearing targets, and the raw-path fallback resolves only through the kernel loader's VIFS1 (the block table is never probed on RV64). Both routes failed, so the app printed `command not found`; `gen_disk.ps1` now stages `/bin/hypha` and `/bin/tool-spawn` into VIFS1, the same class as `/bin/bench`.
- lab/base: reject reactivation of retired configuration epochs so rollback cannot revive stale readiness observations
- base: revoke stale active dispatch authority when newer authenticated cross-job evidence reports an exclusion
- bench: propagate scenario failures, validate intended IPC peers, fix control-loop timeout units, and retain strict serial JSON evidence
- ostd/fs: upgrade `vfs_call` to `service_call_typed_bounded` with bounded timeout, sender-masked receive, and caller generation poisoning after receive errors
- vfs/cellos-fs: fix inode block allocation for inode numbers > 8 with multi-slot superblock tracking, and enforce strict rmdir directory checks
- supervisor: authorize bench role for VFS restart recovery and fix 3-byte frame parsing in hostile backend recovery
- kernel/ostd: keep grant-owner handles task-local and fix death-subscription bookkeeping quota drift across duplicate watches, delivery, cancellation, and task retirement
- e1000: fail closed on DMA authorization/OOM and program rings/descriptors with authorized IOVAs
- nvme: retain authorized IOVAs for queue bases, Identify PRPs, and sector I/O buffers
- drivers: publish initialized state before registration and fail closed when registration is rejected
- pcie: preserve exclusive Driver Cell BDF ownership and DMA pins across competing probes or unacknowledged IOMMU publication/teardown, with spec-correct RISC-V IODIR/IOTINVAL/IOFENCE batches that order prior device reads and writes before reclamation
- x86_64: preserve user state and balance CPL3 GS/PKRU across IDT, suspended SYSCALL, fresh IRET, and scheduler switches
- x86_64: correct the bootstrap SysV stack phase with an 8-byte synthetic bottom-frame slot before tail-entering Rust
- tools: validate MemInfo frame conservation and nonzero page size before independently rounding displayed KiB values
- kernel: clean completion waiter lifecycle safely
- boot: reject snapshots from mismatched RAM bases
- build: align cross-target CI and image generation
- hypha: plaintext transport workaround for net cell TLS crash
- hypha: NetClient.tcp_send now handles Data reply correctly
- hypha: foreground shell spawn now waits on child via sys_wait
- net: boot loop caused by wrong WaitForEvent tick unit
- lua: pcall binding, picolibc link, heap sbrk stub
- doom: posix fseek/ftell, vsnprintf precision, fatfs short-read
- embedded-fs: emit FAT16 to fix CorruptedFileSystem on mount
- rv32: gate rv64 module behind target_arch check
- x86_64: AT&T syntax for global_asm, HHDM PDPT NX bit
- git: untrack .logs/hook-log.jsonl causing perpetual dirty state

---

## [0.2.1] - 2026-06-08

### 🚀 Improvements
- viui: RenderCtx bundles canvas and FontContext for paint calls
- viui: FontContext with GlyphAtlas and 8x8 bitmap fallback
- viui: touch events added to Event enum
- viui: ProgressBar, Slider, TouchArea widgets
- viui: Animatable trait, Tween, easing module, AnimatedSignal
- viui: GpuCommandBuffer as struct field for allocation reuse
- readme: accurate build targets and correct GitHub URL

### 🐛 Fixes
- viui: Slider returns subscribe handle from collect_dirty_handles
- viui: ProgressBar label uses font-aware char_width

---

## [0.2.0] - 2026-05-01 "Mycelium Alpha"

### 🚀 Improvements
- rv64: SV39 paging, PLIC, SBI, UART, ELF loader with PIE
- kernel: basic shell, VirtIO block device, VirtIO keyboard
- hal: AArch64, x86_64, RV32, AArch32 HAL implementations
- security: STRIDE threat model and QEMU CI boot test

---

[Unreleased]: https://github.com/dxsl-org/Cellos/compare/v0.2.1...HEAD
[0.2.1]: https://github.com/dxsl-org/Cellos/compare/v0.2.0...v0.2.1
[0.2.0]: https://github.com/dxsl-org/Cellos/releases/tag/v0.2.0
