[package]
name = "ostd"
version = "0.2.0"
edition = "2021"
license = "MPL-2.0"

[dependencies]
types = { path = "../types" }
api = { path = "../api" }
spin = { workspace = true }
linked_list_allocator = "0.10"

[features]
default = []
