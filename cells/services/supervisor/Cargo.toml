[package]
name    = "supervisor"
version = "0.1.0"
edition = "2021"
license.workspace = true

[[bin]]
name = "supervisor"
path = "src/main.rs"

[dependencies]
types = { path = "../../../libs/types", version = "0.1.0" }
api   = { path = "../../../libs/api", version = "0.1.0" }
ostd  = { path = "../../../libs/ostd", version = "0.2.0" }

[features]
default = []
# Non-production SupervisorCap control used only by hostile backend recovery images.
hostile-backend-recovery = ["api/hostile-backend-recovery"]

[build-dependencies]
cell-build = { path = "../../../libs/cell-build", version = "0.2.0" }
