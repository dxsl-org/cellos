[package]
name = "hypha-tool-sys"
version = "0.1.0"
edition = "2021"
license = "MPL-2.0"

# Hypha tool-sys — system introspection tool cell (P3).
# Provides list_cells (sys_get_procs), sys_info, lookup_service.
# No SpawnCap; read-only OS inspection.

[dependencies]
ostd        = { path = "../../../../../libs/ostd", version = "0.2.0" }
api         = { path = "../../../../../libs/api", version = "0.1.0" }
agent-proto = { path = "../../../../../libs/agent-proto", version = "0.1.0" }
postcard    = { version = "1", default-features = false, features = ["alloc"] }

[build-dependencies]
cell-build = { path = "../../../../../libs/cell-build", version = "0.2.0" }
