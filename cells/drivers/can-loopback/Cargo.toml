[package]
name = "driver-can-loopback"
version = "0.1.0"
edition = "2021"
license.workspace = true

[dependencies]
hal-can = { path = "../../../hal/traits/can", version = "0.1.0" }

[build-dependencies]
cell-build = { path = "../../../libs/cell-build", version = "0.2.0" }
